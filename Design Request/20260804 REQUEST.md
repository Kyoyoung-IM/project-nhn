# 기획자 요청 및 데이터 이슈 — 2026-08-04

- 기록 구간: 2026-08-04 04:00 KST ~ 2026-08-05 03:59:59 KST
- 기준 데이터 테이블: `https://docs.google.com/spreadsheets/d/1DNYhAyOK-8hTO4hKY_9cGGzvM-DoM99ttiObfkve59s/edit?usp=sharing`
- 데이터 테이블은 읽기 전용이며 Codex가 직접 수정하지 않는다.
- 이전 날짜 요청 파일은 읽기 전용 이력으로 취급한다.
- 새 이슈에는 발견 위치, 문제, 기획자 요청, 현재 임시 처리, 영향과 상태를 기록한다.
- 상태는 `기획 확인 요청`, `임시 처리 중`, `반영 완료`, `요청 철회` 중 하나를 사용한다.

## 기획자 전달용 최종 목록

아래 7개 항목이 현재 기획자에게 전달할 미해결 요청이다. 위쪽일수록 실제 조작·경제·승리 조건에 직접 영향을 주는 항목이다.

| 우선순위 | 이슈 | 요청할 결정 | 현재 임시 처리 |
| ---: | --- | --- | --- |
| 1 | `REQ-DESIGN-001` 층간 머지 | 층간 이동·머지 허용 여부와 결과 위치 | 같은 층에서만 이동·머지 |
| 2 | `REQ-DESIGN-002` 밤의 설치 타워 조작 | 밤의 이동·설치 타워 간 머지 허용 여부 | 둘 다 금지, 상점 카드 구매 머지만 허용 |
| 3 | `REQ-DATA-007` 판매 반환 비율 | 정식 필드·비율과 머지 타워 판매가 공식 | 누적 구매가의 50% 반환 |
| 4 | `REQ-DATA-008` 보스 보상 | `boss1.rewardGold=555555` 의도 여부 | 원본 값 그대로 사용 |
| 5 | `REQ-DESIGN-003` 보스 웨이브 구성 | `wave4` 보스 4기와 최종 클리어 구성이 의도인지 | 보스 4기 포함 전장 전체 적 처치 시 승리 |
| 6 | `REQ-DESIGN-004` `failAllowedMonster` | 필드 의미와 향후 사용 여부 | 호환용 보존, 패배 판정에는 미사용 |
| 7 | `REQ-DESIGN-005` 상점 카드 수 | 데이터 5종에 맞춘 카드 5장이 최종인지 | 카드 5장 유지 |

## 신규 요청

### REQ-DESIGN-001 · 서로 다른 층 사이의 터렛 머지 허용 여부

- 발견 위치: 터렛 배치·이동·머지 규칙
- 이슈: 동일 종류·Tier 터렛의 머지 규칙은 확정됐지만, 서로 다른 층에 설치된 터렛끼리 머지할 수 있는지는 확정되지 않았다.
- 기획자 요청: 층간 드래그 이동과 층간 머지를 허용할지, 허용한다면 결과 터렛이 어느 층과 슬롯에 남는지 확정해 달라.
- 현재 임시 처리: 설치 터렛은 같은 층 안에서만 드래그할 수 있으며, 같은 층의 동일 터렛끼리만 머지한다.
- 영향: 서로 다른 층에 같은 터렛이 있어도 현재는 합칠 수 없다.
- 상태: `기획 확인 요청` — 2026-08-04 사용자 지시로 임시 층간 머지 불가

### REQ-DESIGN-002 · 밤 단계의 설치 터렛 이동·설치 터렛 간 머지 허용 여부

- 발견 위치: 낮·밤 단계별 조작 규칙
- 이슈: 밤에 설치된 터렛끼리 이동·머지할 수 있는지는 확정되지 않았다. 다만 상점 카드로 동일 터렛을 구매해 바로 머지하는 조작은 별도 허용하기로 했다.
- 기획자 요청: 밤에도 설치 터렛 간 이동 또는 머지를 허용할지 확정하고, 상점 카드 구매 머지만 예외로 허용하는 현재 구분이 최종 규칙인지 확인해 달라.
- 현재 임시 처리: 설치 터렛의 이동과 설치 터렛 간 머지는 낮에만 허용한다. 상점 카드를 동일 `turretId`·Tier 터렛에 드롭하는 구매 머지는 낮과 밤 모두 허용하고 성공할 때만 카드 가격을 차감한다.
- 영향: 밤에는 배치된 두 터렛을 합칠 수 없지만, 상점에 같은 터렛이 등장하면 구매와 동시에 상위 Tier로 머지할 수 있다.
- 상태: `기획 확인 요청` — 2026-08-04 사용자 지시로 밤에는 상점 드래그 머지만 임시 허용

### REQ-DATA-007 · 터렛 판매 반환 비율 필드 누락

- 발견 위치: `define`, 터렛 판매 규칙
- 이슈: 설치 터렛 판매 시 누적 구매가에서 반환할 비율을 나타내는 필드가 최신 기준 데이터 테이블에도 없다.
- 기획자 요청: 판매 반환 비율을 확정하고 `define`에 `sellRefundRate` 또는 동등한 필드를 추가해 달라. 머지 터렛은 구성 터렛에 실제 투입된 구매 비용을 합산하는 방식이 최종 규칙인지도 확인해 달라.
- 현재 임시 처리: 로컬 `prototypeExtensions.sellRefundRate=0.5`를 사용하고 판매가는 `floor(누적 구매가 × 0.5)`로 계산한다.
- 영향: Tier 2 이상 터렛도 데이터의 `basePrice=-1`을 사용하지 않고 실제 투입된 전체 구매 비용의 절반을 반환한다.
- 상태: `기획 확인 요청` — 2026-08-04 사용자 승인으로 임시 누적 구매가 50% 반환 적용

### REQ-DATA-008 · `boss1.rewardGold=555555` 값 확인

- 원본 참조: `20260803 REQUEST.md`의 `REQ-DATA-005`
- 발견 위치: `monster.boss1.rewardGold`
- 이슈: 최신 데이터 테이블 재확인 뒤에도 보스 처치 보상이 `555555`로 유지되어 일반 몬스터의 `1~3` 골드와 차이가 매우 크다.
- 기획자 요청: 의도된 무제한 경제 전환용 보상인지, 임시 테스트 값인지 확인하고 최종 값을 제공해 달라.
- 현재 임시 처리: 읽기 전용 원본 값 `555555`를 수정하지 않고 로컬 데이터와 게임 보상에 그대로 사용한다.
- 영향: 보스 한 기 처치 뒤 골드 제약이 사실상 사라진다. 보스 웨이브 중에도 상점이 열려 있어 남은 전투의 구매·리롤 밸런스에 직접 영향을 준다.
- 상태: `기획 확인 요청`

### REQ-DESIGN-003 · 마지막 보스 웨이브의 보스 4기 구성 확인

- 발견 위치: `spawntable.wave4`, 최종 승리 조건
- 이슈: 최신 데이터는 `wave4`에 `boss1` 4기를 배치한다. 보스 한 기 처치 즉시 승리 규칙은 철회됐고 현재는 마지막 웨이브 스폰 완료 뒤 전장에 남은 모든 적을 처치해야 승리한다.
- 기획자 요청: 최종 웨이브에 동일 보스 4기를 배치하는 것이 의도된 구성인지 확인하고, 의도되지 않았다면 최종 SpawnTable 구성을 제공해 달라.
- 현재 임시 처리: 데이터 행을 그대로 사용해 보스 4기를 스폰하며, 이전 웨이브 잔존 적까지 포함해 전장의 모든 적을 처치하면 승리한다.
- 영향: 마지막 전투 난이도와 플레이 시간이 보스 4기 구성에 크게 좌우된다.
- 상태: `기획 확인 요청`

### REQ-DESIGN-004 · `failAllowedMonster`의 최종 의미와 사용 여부

- 발견 위치: `define.failAllowedMonster`, 패배 조건
- 이슈: 필드는 데이터 호환용으로 존재하지만 실시간 필드 수, 웨이브 종료 잔존 수, 최심부 도달 허용 수 중 어떤 의미인지 확정되지 않았다.
- 기획자 요청: 이 필드를 최종 게임에서 사용할지, 사용한다면 집계 대상과 패배 판정 시점을 명시해 달라. 사용하지 않는다면 제거 또는 미사용 필드로 확정해 달라.
- 현재 임시 처리: 값을 읽고 보존하되 게임 판정에는 사용하지 않는다. 몬스터 한 기라도 B3 왼쪽 최종 도달 좌표에 닿으면 즉시 패배한다.
- 영향: 현재는 누적 허용 수나 웨이브 단위 유예가 전혀 없다.
- 상태: `기획 확인 요청` — 사용자 지시로 당분간 미사용

### REQ-DESIGN-005 · 최종 상점 카드 수 확인

- 발견 위치: 상점 UI, `ShopGacha`, 터렛 5종
- 이슈: 초기 화면 스케치는 상점 카드 4개였으나 데이터 테이블에는 상점용 Tier 1 터렛 5종이 각각 0.2 확률로 존재하고 현재 UI도 카드 5장으로 구현되어 있다.
- 기획자 요청: 한 번에 표시할 최종 카드 수를 5장으로 확정할지 확인해 달라. 4장이라면 5종 추첨 확률은 유지한 채 표시 슬롯만 4개로 줄이는지 함께 명시해 달라.
- 현재 임시 처리: TFT형 한 줄 UI에 카드 5장을 표시한다.
- 영향: 리롤 1회당 선택지 수, 화면 폭, 구매·머지 기대값에 영향을 준다.
- 상태: `기획 확인 요청`

## 해결된 요청

- `20260803 REQ-DATA-001`: `rerollPlusCost=5`가 테이블에 추가되어 원본 값 반영 완료.
- `20260803 REQ-DATA-002`: `totalWaveCount=4`로 갱신되어 `wave1`~`wave4`와 일치, 반영 완료.
- `20260803 REQ-DATA-003`: `wave3 spawnOrder`가 고유 순서로 정리되어 오름차순 실행 반영 완료.
- `20260803 REQ-DATA-004`: 기획자 확정으로 몬스터 공격을 사용하지 않으며 관련 전투 필드 요청 종료.
- `20260803 REQ-DATA-006`: `spawnOrderInterval=0.5` 반영 완료. `waveTimeSec=50`은 잔존 적을 유지한 채 다음 웨이브를 시작하는 강제 전환 시간으로 확정.
- `20260804 기획자 전달사항`: 몬스터는 타워를 공격하지 않으며 현재 타워는 체력·사망이 없는 고정 오브젝트로 반영 완료.
- `20260804 보스 승리 규칙`: 보스 한 기 처치 즉시 승리를 철회하고, 마지막 보스 웨이브 스폰 완료 뒤 이전 웨이브 잔존 적을 포함한 전장 전체 적 처치 시 승리하도록 반영 완료.
- `20260804 공격 방식`: 근접 히트스캔, 원거리 콩알 투사체, 지속 화염방사·화상, 둔화 눈덩이·둔화, 기절 충전·히트스캔 낙뢰·기절 이펙트로 반영 완료.
- `20260804 투사체 층 규칙`: 대상이 층 이동을 시작하면 해당 투사체와 진행 중 화염방사는 추적을 중단하고 피해 없이 소멸하도록 반영 완료.
- `20260805 데이터 재확인`: `Define`, `Turret`, `Monster`, `SpawnTable`, `ShopGacha`의 원본 값이 2026-08-04 반영본과 동일함을 읽기 전용으로 확인하고 메타데이터 검증일만 갱신 완료.
