# 기획자 요청 및 데이터 이슈 — 2026-08-05

- 기록 구간: 2026-08-05 04:00 KST ~ 2026-08-06 03:59:59 KST
- 기준 데이터 테이블: `https://docs.google.com/spreadsheets/d/1DNYhAyOK-8hTO4hKY_9cGGzvM-DoM99ttiObfkve59s/edit?usp=sharing`
- 데이터 테이블은 읽기 전용이며 Codex가 직접 수정하지 않는다.
- `20260804 REQUEST.md`는 이전 04:00 구간의 읽기 전용 이력으로 유지한다.
- 이전 요청에 대한 답변과 후속 상태는 원본 파일을 수정하지 않고 이 문서에서 원본 파일명과 이슈 ID를 참조해 기록한다.

## 기획자 전달용 확인 목록

| 우선순위 | 이슈 | 확인할 내용 | 현재 처리 |
| ---: | --- | --- | --- |
| 1 | `20260804 REQ-DATA-007` 판매 규칙 | “판매 규칙은 없습니다”가 판매 기능 제거인지, 데이터 필드 없이 기존 50% 규칙 유지인지 | 기존 판매 기능과 50% 임시 반환을 변경하지 않고 보류 |
| 2 | `20260804 REQ-DESIGN-001` 다른 터렛 머지 | “다른 터렛”이 서로 다른 `turretId`까지 뜻하는지, 그렇다면 결과 터렛 ID를 정하는 공식 | 동일 `turretId`·Tier의 층간 머지만 반영 |
| 3 | `20260804 REQ-DESIGN-005` 카드 수 자동화 | `ShopGacha` 행 수만큼 카드를 생성하는 배치안과 2티어 직접 판매 행 표시 방식 승인 | UI 예상 배치 승인 전 기존 5장 유지 |

## 신규 확인 요청

### REQ-DESIGN-006 · 판매 규칙 답변의 적용 범위

- 원본 참조: `20260804 REQUEST.md`의 `REQ-DATA-007`
- 전달 답변: `판매 규칙은 없습니다!`
- 충돌 내용: 현재 확정 문서와 구현에는 낮에 설치 터렛을 상점 하단으로 드래그해 누적 구매가의 50%를 돌려받는 판매 기능이 있다.
- 기획자 요청: 위 답변이 **판매 기능 자체를 제거**하라는 의미인지, 또는 **데이터 테이블에 정식 판매 비율 필드를 추가하지 않고 현재 프로토타입 규칙을 유지**한다는 의미인지 선택해 달라.
- 현재 처리: 기존 판매 UI·기능과 로컬 `sellRefundRate=0.5`를 유지하며 새 답변을 임의 적용하지 않는다.
- 상태: `기획 확인 요청`

### REQ-DESIGN-007 · 서로 다른 터렛 종류 간 머지 결과

- 원본 참조: `20260804 REQUEST.md`의 `REQ-DESIGN-001`
- 전달 답변: `다른층, 다른 터렛과도 머지가 가능`
- 확인 필요: 서로 다른 층의 **동일 `turretId`·Tier 터렛** 머지는 기존 `nextTurretId`로 결과가 명확하다. 서로 다른 `turretId`끼리도 머지한다는 의미라면 결과 터렛 종류·Tier를 결정할 데이터와 우선순위가 없다.
- 기획자 요청: “다른 터렛”이 단순히 다른 터렛 인스턴스를 뜻하는지, 서로 다른 타입(`MELEE`+`DOT` 등)까지 뜻하는지 확정해 달라. 서로 다른 타입도 허용한다면 조합별 결과 ID 규칙을 제공해 달라.
- 현재 처리: 동일 `turretId`·동일 Tier끼리만 머지하며, 서로 다른 층과 밤 머지는 허용한다.
- 상태: `기획 확인 요청`

### REQ-UI-001 · 데이터 기반 상점 카드 수 자동 배치 승인

- 원본 참조: `20260804 REQUEST.md`의 `REQ-DESIGN-005`
- 전달 답변: 현재는 5장이지만 테이블에 따라 자동 변경하고, 향후 Tier 2를 상점에서 바로 판매할 수 있게 한다.
- 구현 제안: `ShopGacha`의 유효 행 수를 카드 개수로 사용하고 각 행의 `turretIndex`가 Tier 1인지 여부와 관계없이 해당 터렛 데이터를 표시·판매한다. 일반 모드는 그 행들로 같은 개수만큼 확률 추첨하고 테스트 모드는 유효 행을 행 순서대로 모두 표시한다.
- 예상 배치도:

```text
[골드/리롤] [카드 1] [카드 2] ... [카드 N]
             └──── ShopGacha 유효 행 수에 맞춰 동일 높이로 자동 축소·확장 ────┘
```

- 기획자 요청: 카드가 1920×1080 하단 폭을 넘을 때 `카드 폭 자동 축소`와 `가로 스크롤` 중 어느 방식을 사용할지 포함해 위 배치안을 승인해 달라.
- 현재 처리: UI 구성 변경 승인 전 기존 카드 5장과 현 배치를 유지한다.
- 상태: `기획 확인 요청`

## 해결된 요청

- `20260804 REQ-DESIGN-001` 일부 해결: 동일 `turretId`·Tier 터렛은 서로 다른 층에서도 머지하도록 반영. 결과는 드롭 대상 슬롯과 층에 남긴다. 서로 다른 타입 머지는 `REQ-DESIGN-007`로 재확인한다.
- `20260804 REQ-DESIGN-002`: 밤에도 설치된 동일 `turretId`·Tier 터렛끼리 수동 머지를 허용. 밤의 빈 슬롯 이동과 판매는 계속 금지한다.
- `20260804 REQ-DATA-008`: `boss1.rewardGold=555555`는 의도된 값으로 확인되어 읽기 전용 원본과 현재 구현을 유지한다.
- `20260804 REQ-DESIGN-003`: 보스 웨이브의 보스 수가 데이터 입력 실수였고 1기로 수정됐음을 전달받았다. 데이터 전용 동기화 작업에서 최신 원본을 반영한다. 웨이브·스폰 구조와 최종 전장 전체 적 처치 승리 규칙은 유지한다.
- `20260804 REQ-DESIGN-004`: `failAllowedMonster`는 향후 N기 허용 규칙 확장을 위해 데이터 구조에 보존한다. 현재는 한 기라도 최심부 도달 시 즉시 패배하며 필드 값은 판정에 사용하지 않는다.
- `20260804 REQ-DESIGN-005` 일부 해결: 현재 카드 수 5장은 확정. 테이블 기반 자동 개수와 Tier 2 직접 판매 대응은 `REQ-UI-001`의 배치 승인 후 구현한다.
