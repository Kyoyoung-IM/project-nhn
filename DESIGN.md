# NAN 2026 머지 디펜스 게임 기획

## 1. 문서 역할

이 문서는 게임의 목표, 핵심 루프, 전장, 전투, 상점, UI, 데이터 기획, 밸런스 초안과 미확정 항목을 관리한다.

- 개발 절차와 작업 지침은 `AGENTS.md`를 따른다.
- 데이터 테이블의 누락·충돌·기획자 요청과 임시 처리 상태는 `Design Request/`의 현재 활성 `YYYYMMDD REQUEST.md`를 따른다.
- 사용자 답변으로 확정된 세부 규칙과 날짜별 변경 이력은 `docs/GAME_SPEC.md`를 함께 참고한다.
- 이 문서와 `docs/GAME_SPEC.md`가 충돌하면 임의로 결정하지 않고 사용자에게 확인한다.
- 프로토타입 수치는 최종 밸런스가 아니며 교체 가능한 `PLACEHOLDER` 데이터로 취급한다.

## 2. 게임 개요

- 장르: 2D 머지 디펜스
- 화면: 플랫포머처럼 보이는 2D 측면 뷰
- 목표 플랫폼: 브라우저 Web 빌드
- 핵심 목표: 터렛을 구매·배치·관리하며 자동 전투 웨이브를 방어한다.
- 패배 조건: 몬스터가 가장 깊은 전투층의 왼쪽 경로 끝 좌표에 도달한다. 별도의 코어 오브젝트나 그래픽은 표시하지 않는다.
- `failAllowedMonster`는 원본 데이터 호환을 위해 유지하지만 현재 패배 판정에는 사용하지 않는다.

## 3. 핵심 플레이 흐름

1. 실행하면 타이틀 화면을 먼저 표시하고 `게임 시작`으로 일반 플레이에 진입한다.
2. 게임 시작 전에 `prepareTimeSec` 동안 `낮` 단계를 한 번 진행한다.
3. 낮과 밤 모두 상점 카드를 빈 슬롯으로 드래그해 터렛을 구매·설치할 수 있다.
4. 낮과 밤 모두 상점을 리롤할 수 있다.
5. 낮에는 설치된 터렛을 같은 층의 빈 슬롯으로 이동할 수 있고, 낮과 밤 모두 동일 종류·Tier 터렛에 드롭해 층과 관계없이 머지할 수 있다.
6. `밤`이 시작되면 몬스터는 공격하지 않고 고정 경로를 이동하며, 터렛만 자동으로 공격한다. 밤의 설치 터렛 드래그는 머지에만 사용하고 빈 슬롯 이동에는 사용하지 않는다.
7. 각 웨이브는 `waveTimeSec`(현재 80초)를 기준시간으로 사용한다. 마지막 웨이브 전까지 기준시간이 끝나면 기존 웨이브의 잔존 몬스터와 진행 중 투사체를 모두 유지한 채 낮 단계를 거치지 않고 다음 웨이브를 즉시 추가로 시작한다.
8. 보스 한 기 처치로 즉시 승리하지 않는다. 마지막 보스 웨이브의 모든 몬스터가 생성되고, 이전 웨이브에서 누적된 잔존 몬스터를 포함해 전장에 남은 적을 전부 처치해야 승리한다.
9. 몬스터가 최심부 도달 지점에 도달하면 즉시 패배한다.

AI 공세 디렉터는 후순위 선택 기능이다. 사용자가 별도로 요청하기 전에는 구현하지 않으며, 기본 게임 완성과 Web 제출 안정성을 우선한다. 구현할 경우 규칙 기반 또는 유틸리티 기반 시스템으로 명시한다.

## 4. 전장과 몬스터 이동

- 지상층은 몬스터가 지하로 내려가는 진입 연출을 위한 비전투 공간이다.
- 지상층은 향후 기능 확장을 고려해 전투층과 분리된 구간으로 유지한다.
- 실제 전투는 지하 3개 층에서 진행한다.
- 모든 지하 전투층에서 몬스터는 오른쪽 입구에서 왼쪽 출구로 이동한다.
- 한 층의 왼쪽 출구에 도달하면 하강 연출 후 다음 층의 오른쪽 입구에서 이동을 재개한다.
- 최심부 코어는 지하 3층의 왼쪽 경로 끝에 둔다.
- 실제 플랫포머 물리, 점프, 사다리 탐색 AI는 사용하지 않는다.
- 이동은 고정 웨이포인트 또는 `Path2D`와 상태 전환으로 구현한다.
- 기본 이동 상태는 `SPAWNING -> WALKING -> DESCENDING -> WALKING -> EXIT/DEAD`로 구성한다.
- 층간 하강은 상태 전환과 짧은 애니메이션으로 표현한다.
- 적의 지상 스폰·중간·하강 지점, B1~B3 입구·출구와 최종 패배 지점은 `game/scenes/battlefield_layout.tscn`의 `MonsterRoute` Marker2D로 관리해 Godot 에디터에서 직접 배치한다. 고정 전체 뷰 화면 기준 지상과 각 지하층의 진입점은 오른쪽 밖 `x=1980`, 출구는 왼쪽 밖 `x=-60`에 두어 적이 맵 밖에서 걸어 들어와 반대편 맵 밖으로 나가게 한다.
- 층별 타워 슬롯 15개도 같은 편집 씬에서 관리한다. 런타임 코드는 장면의 위치를 읽을 뿐 전장 제작 좌표를 별도로 하드코딩하지 않는다.

## 5. 터렛과 전투

- 각 지하 전투층에는 고정 터렛 슬롯 5개를 둔다.
- 각 전투층의 슬롯 5개는 중앙 50% 균등 배치안에서 화면 너비의 20%인 `384px`만큼 왼쪽으로 평행 이동한다.
- 세 전투층은 동일하게 화면 X `96/336/576/816/1056`을 사용하고 `240px` 간격을 유지한다.
- 몬스터 속도와 터렛 사거리 데이터는 좌표 재배치 전 값을 유지한다.
- 이 분기 버전의 터렛은 체력과 사망 상태가 없는 고정 불파괴 오브젝트다.
- 기획자 확정에 따라 몬스터는 공격하지 않으며, 터렛을 공격하거나 가로막히지 않고 이동 경로만 진행한다.
- 낮에는 설치된 터렛을 마우스로 드래그해 같은 층의 빈 슬롯으로 이동할 수 있다.
- 설치된 터렛은 다른 층으로 이동할 수 없다.
- 밤에는 설치된 터렛을 드래그해 빈 슬롯으로 이동할 수 없지만, 설치 터렛끼리 머지할 수 있다.
- 낮과 밤 모두 동일한 `turretId`와 Tier를 가진 터렛 하나를 같은 층 또는 다른 층의 다른 터렛 위에 드롭하면 두 터렛을 하나의 상위 Tier로 머지한다.
- 머지는 플레이어가 직접 드래그해 겹쳤을 때만 실행하며, 동일한 터렛 두 개가 배치되어 있어도 자동으로 합치지 않는다.
- 머지가 성립하면 드래그한 터렛과 대상 터렛을 소비하고 드롭 대상 슬롯과 층에 `nextTurretId`가 가리키는 상위 터렛을 배치한다.
- 서로 다른 종류·Tier의 터렛과 `nextTurretId=-1`인 Tier 4 터렛은 결과 규칙이 없으므로 머지할 수 없고 원래 위치로 되돌린다. `20260804 REQ-DESIGN-001` 답변의 “다른 터렛”이 서로 다른 `turretId`까지 뜻하는지는 `Design Request/20260805 REQUEST.md`에서 재확인한다.
- 머지된 터렛의 공격력·공격 주기·사거리·CC 등 전투 스탯은 상위 `turretId`의 데이터 테이블 값을 사용한다.
- `MELEE`, `RANGED`, `DOT`, `SLOW`, `STUN`은 각각 Tier 1~4 전용 투명 스프라이트를 사용한다. 투명 캔버스 전체가 아니라 실제 불투명 그림 면적을 기준으로 크기를 정규화하며, 모든 유형이 머지 Tier 순서대로 일정하게 커진다. 낮고 넓은 `DOT` Tier 4는 Tier 3보다 확실히 크게 인식되도록 본체와 불꽃에 별도 확대 보정을 적용한다.
- `DOT`의 모든 Tier 불꽃과 `STUN` Tier 4의 번개는 본체 이미지에서 분리된 독립 이펙트 노드로 표시한다. `DOT` Tier 3·4는 `후방 본체 → 불꽃 → 전방 본체` 순으로 레이어를 분리해 불꽃이 화로 내부에서 솟는 콘셉트 아트의 깊이를 재현한다. 이펙트의 실제 그림 중심을 본체 중심에 맞춰 결합하고 본체 교체 없이 점멸·크기 변화·공격 이펙트 전환을 조정할 수 있어야 한다.
- 구름 형태의 `STUN` 타워는 발판 기준 96px 위에서 미세하게 상하 부유한다. 이는 이전 144px 부유 높이의 2/3이며 Tier 4 번개 이펙트도 본체와 같은 높이 변화를 따른다.
- 설치된 아군 타워는 타입·Tier별 2프레임 대기 애니메이션을 반복한다. 공격할 때는 타입·Tier별 2×2 시트를 좌상→우상→좌하→우하 순서로 한 번 재생한 뒤 대기 애니메이션으로 복귀하며, Web에서도 같은 순서와 접지 높이를 유지한다.
- 머지 완료 시에는 스프라이트 교체와 함께 기존의 짧은 승급 링·빛 점 이펙트를 유지한다.
- 상점 카드에서 같은 `turretId`·Tier의 설치 터렛 위로 직접 드롭하는 구매 머지는 낮과 밤 모두 가능하다.
- 상점 구매 머지는 설치 터렛 하나와 구매 카드 하나를 소비해 대상 슬롯에 `nextTurretId`의 상위 터렛을 남긴다.
- 상점 카드 설치와 설치 터렛 이동·머지는 슬롯 중심에서 120px 이내에 드롭하면 해당 슬롯을 대상으로 판정한다. 이는 슬롯 중심 간격 240px의 절반으로, 인접 슬롯 판정이 겹치지 않는 범위에서 기존 70px 판정을 약 1.7배 확대한 값이다.
- 설치된 터렛을 제거해 골드로 되돌리는 조작은 제공하지 않는다. 유효한 이동·머지 대상이 아닌 곳에 드롭하면 원래 슬롯으로 복귀한다.
- 현재 터렛 유형 초안은 `MELEE`, `DOT`, `STUN`, `SLOW`, `RANGED`다.
- 터렛은 같은 전투층의 적만 대상으로 삼으며, 사거리는 화면 연출용 수직 간격을 제외한 수평 거리로 판정한다.
- `MELEE`는 투사체 없이 즉시 판정하는 히트스캔 공격이며, 피격 몬스터 위치에 짧은 할퀴기 이펙트를 표시한다.
- `DOT`는 일반 투사체 노드를 사용하지 않고 타워의 발사구에서 대상 방향으로 짧게 이어지는 화염방사 공격을 사용한다. 화염 PNG의 원래 좌→우 방향을 유지해 좁은 쪽이 포구에서 시작하고, 이 이펙트에 한해 원본 종횡비를 보존하지 않고 가로로 늘려 넓은 쪽 불투명 영역이 생성 즉시 대상의 실제 이미지 중심을 지나도록 겹친 채 공격 표시 동안 연결되어야 한다. 사거리 안의 지정 대상에게 화염이 닿는 시점에 기본 피해와 화상 지속 피해를 적용한다. 공격 사거리는 기획자 확정에 따라 `rangeValue`를 사용하며 현재 `1.5칸`이다.
- `SLOW`는 눈덩이 투사체를 발사하고 명중 시 기본 피해와 둔화 상태이상을 적용한다.
- `RANGED`는 초록색 콩알 투사체를 발사하고 명중 시 기본 피해를 적용한다.
- `STUN`은 포탑에 짧은 전기 충전 이펙트를 먼저 표시한 뒤 히트스캔으로 공격한다. 대상 머리 위에는 낙뢰 이펙트를, 기절 상태에는 헤롱헤롱 이펙트를 표시한다.
- 투사체형 공격은 발사 즉시가 아니라 투사체가 대상에 도달한 시점에 기존 데이터의 피해와 상태이상을 적용한다.
- 발사된 투사체와 진행 중인 화염방사는 발사 당시 전투층을 벗어나지 않는다. 대상이 층간 하강을 시작하거나 다른 층으로 이동하면 추적을 중단하고 피해 없이 소멸한다.
- `SLOW`와 `RANGED` 투사체 본체 이미지에는 고정된 트레일을 포함하지 않는다. 트레일은 런타임에서 실제 투사체 이동 궤적을 따라 생성한다. `DOT` 화염방사는 별도의 길이·방향 가변 연출로 처리한다.
- 몬스터는 설치 터렛보다 앞쪽 레이어에 표시하고 투사체와 피격 이펙트는 몬스터보다 앞에 표시한다.
- 몬스터는 `game/assets/enemy/`의 타입별 투명 스프라이트를 사용한다. 일반형의 현재 표시 배율을 공통 기준으로 삼고 속도·탱크형에도 같은 배율을 적용해, 보스를 제외한 몬스터 사이의 실제 불투명 그림 픽셀 크기 차이를 그대로 표시한다. 보스는 종횡비를 유지한 채 한 전투층의 세로 간격보다 약간 작은 기존 높이를 유지한다.
- 몬스터 경로 좌표는 모든 타입의 실제 발끝이 새 배경의 지상·B1·B2·B3 발판에 닿는 접지선으로 사용한다.
- 몬스터가 직접 피해를 받으면 타입별 피격 이미지를 0.25초 표시하고, 표시 중 다시 직접 피해를 받으면 남은 표시 시간에 0.1초를 더한다. 화상으로 받는 주기 피해는 피격 이미지 표시와 시간 가산에서 제외한다.
- 몬스터가 사망하면 타입별 5프레임 사망 애니메이션을 0.6초 동안 표시한 뒤 전장에서 제거한다. 사망 즉시 이동·공격 대상·상태이상 판정에서는 제외하며 처치 보상과 웨이브 판정은 기존처럼 즉시 처리한다.
- 몬스터 체력 바는 피해를 받기 전에는 숨기며, 첫 유효 피해를 받은 순간부터 실제 그림의 머리 위에 얇은 막대로 표시한다.
- 각 터렛 유형은 Tier 1부터 Tier 4까지의 머지 트리를 가진다.
- 상점에는 Tier 1 터렛만 등장한다.

## 6. 상점과 경제

- 화면 하단에는 TFT 챔피언 상점 형태의 터렛 카드 UI를 둔다.
- 기본 카드에는 별도의 유색 이미지 배경 판넬 없이 Tier 1 터렛 투명 스프라이트, 이름, 가격을 표시한다.
- 마우스 호버 시 카드 전체를 약 80% 불투명한 어두운 색으로 딤드하고, 그 위에 터렛 이름·효과·상세 능력치를 중앙 배치한다. 호버 상세에서는 터렛 이미지와 구매 가격을 표시하지 않으며 구매 가격은 기본 카드에서만 확인한다. 호버 상세 레이어는 구매 완료 표시보다 위에 그려 항상 상세 정보가 우선 표시된다.
- 골드가 부족한 카드는 전체를 딤드하지 않고 원래 이미지와 색상을 유지한다. 가격 캡슐·동전·가격 글자만 그레이스케일로 표시한다.
- 골드와 리롤 버튼은 상점 카드 옆의 전용 제어 영역에 배치한다.
- 일반 플레이의 상점 카드는 `ShopGacha` 확률에 따라 게임 시작, 초기화, 리롤 시 무작위로 추첨한다.
- 자동 테스트와 디버그 실행은 재현 가능한 고정 RNG 시드를 지원한다.
- 구매는 카드 클릭 후 슬롯 클릭 방식이 아니라 상점 카드를 빈 슬롯으로 드래그하는 방식이다.
- 골드는 유효한 빈 슬롯 설치 또는 호환되는 점유 슬롯 머지가 확정되는 순간에만 차감한다.
- 잘못된 위치, 다른 종류·Tier가 점유한 슬롯 또는 최고 Tier 슬롯에 드롭하면 구매와 골드 차감을 모두 취소한다.
- 상점 카드를 동일 `turretId`·Tier 터렛에 드롭하면 카드 가격을 지불하고 즉시 상위 Tier로 머지한다.
- 상점 구매, 빈 슬롯 배치와 리롤은 낮과 밤 모두 가능하다.
- 리롤 비용은 `rerollCost + rerollPlusCost * 해당 낮의 리롤 실행 횟수`로 계산한다.
- 최신 데이터 테이블의 `rerollPlusCost=0`을 사용하므로 현재 리롤 비용은 실행 횟수와 관계없이 `rerollCost`로 고정된다.
- 현재는 최초 낮 이후 웨이브 사이 낮 단계가 없으므로 리롤 실행 횟수와 비용은 게임 초기화 때만 기본값으로 초기화한다.
- 리롤 가능 상태는 밝은 강조색으로, 골드 부족 상태는 어두운 회색과 명시적인 부족 문구로 구분한다.
- 밤 배경과 색조 전환은 상점 카드의 밝기에 영향을 주지 않으며, 상점 카드는 낮과 밤에 같은 원본 색상으로 표시한다. 구매 완료 상태만 카드 전체를 별도 딤드하며, 골드 부족은 가격 영역의 그레이스케일로만 구분한다.

## 7. 화면과 UI

- 기준 해상도는 `1920×1080`이다.
- 브라우저 크기에 따라 16:9 비율을 유지하며 자동 확대·축소하고 남는 축의 가운데에 배치한다.
- 전장은 상단 지상 진입 구간과 지하 3개 전투층으로 구성한다.
- 전장은 카메라 이동 없이 지상과 B1~B3를 `1920×1080` 한 화면에 항상 함께 보여준다. 배경·몬스터·터렛·전투 이펙트는 가로·세로에 같은 고정 배율을 적용해 원본 비율을 유지하며, HUD와 하단 상점은 기존 크기로 고정한다.
- 지상과 B1~B3의 접지선은 최초 16:9 초안처럼 위에서 아래로 한눈에 구분되도록 배치하고, B3 오브젝트 접촉선은 전투 오브젝트 클리핑 경계 위에 최소 10px를 남긴다.
- 지상 수평 경로의 접촉선은 잔디 표면에 맞춰 월드 Y `1336`으로 두며, 몬스터의 발이 지면에서 떠 보이지 않게 한다.
- 마우스 휠과 키보드 위·아래 방향키는 전장 뷰 이동에 사용하지 않는다.
- 배경은 밝은 초원 지상과 따뜻한 갈색 동굴 3개 층, 하단 광산 창고가 이어지는 캐주얼 판타지 RPG 단면 일러스트를 사용한다.
- 배경의 미적 비율과 자연스러운 발판 위치를 우선하고, 몬스터 경로·터렛 슬롯·입구 같은 게임 오브젝트 좌표를 배경에 맞춘다.
- 세로형 배경 원본은 화면 폭 기준으로 먼저 균일 확대하고, 고정 전체 뷰에서 다시 균일 축소한 뒤 `1920×1080` 기준 영역 밖만 잘라 보여준다. 브라우저 비율에 따라 장식 암벽은 일부 잘릴 수 있지만 세 전투 플랫폼은 모두 한 화면에 유지한다.
- 지상은 광산 입구와 오른쪽 숲 앞의 흙길을 표시하지 않는 연속 잔디 지면이며, 몬스터는 오른쪽 숲에서 생성되어 높낮이 변화 없는 수평 직선으로 안쪽 광산 입구까지 이동한다.
- 지하 B1~B3의 조명은 각각 세로 기둥 하나와 짧은 가로 걸이, 매달린 랜턴 하나로 구성하고 세 기둥의 가로 위치를 맞춘다.
- 대형 황금 광물과 고정식 자동 드릴 에셋은 향후 채굴 기믹 후보로 보관하되, 현재 전장에는 배치하지 않는다.
- 지상과 지하 몬스터의 수평 이동선은 몬스터 중심이 아니라 종류별 더미 도형의 실제 최하단이 지면 또는 플랫폼 두께의 중앙선에 닿도록 배치한다. 정식 몬스터 스프라이트의 예상 크기를 대비해 현재 더미 도형은 1.6배로 표시한다.
- 지상 등장 확대 연출 중에도 몬스터의 발끝을 지면 접촉선에 고정해 공중부양이나 지면 침투가 발생하지 않게 한다.
- 생성 배경에는 캐릭터·몬스터·터렛·UI·문자·방향 화살표를 포함하지 않아 게임 오브젝트와 기능 UI를 분리한다.
- `game/assets/backgrounds/bg.png`를 낮 배경으로, 같은 구도의 `bg_night.png`를 밤 배경으로 사용하며 낮/밤 전환 시 두 텍스처를 짧게 교차 페이드한다. 두 원본은 런타임에서 같은 좌표와 같은 균일 배율로 그린다.
- 밤 배경은 지형과 오브젝트 배치를 유지하고 하늘·달빛·그림자·기존 횃불과 수정 발광만 변화시킨다.
- 낮·밤 배경은 주요 구도와 발판 위치를 유지하되 레퍼런스 수준으로 세부 묘사를 크게 줄이고, 재질별 명암을 약 3~4단계의 넓고 평평한 색면과 단순한 실루엣으로 표현한다.
- 지하의 작은 황금 광석 군집은 층마다 1~3개 범위에서 서로 다른 수·간격·크기·기울기로 배치하며 좌우 대칭을 요구하지 않는다. 종유석도 플랫폼을 침범하지 않는 범위에서 길이와 간격을 불규칙하게 구성한다.
- HUD·상점·버튼은 굵은 짙은 남보라 외곽선, 둥근 모서리와 아래쪽 그림자를 공통으로 사용한다.
- 터렛 설치 위치 UI는 다른 판넬과 구분되는 연두색 계열을 사용하며 기존 슬롯 표시의 가로·세로 크기와 드롭 판정 영역을 각각 2배로 확대한다. 평상시 빈 슬롯은 30% 불투명도로 표시하고 포탑이 설치된 점유 슬롯은 0%로 숨긴다. 설치된 타워를 선택·이동하거나 상점 카드를 구매 드래그하는 동안에는 유효한 빈 슬롯과 머지 대상을 60% 불투명도로 강조하고, 드래그 취소·배치 완료 즉시 빈 슬롯은 기본 30%, 점유 슬롯은 0%로 복원한다.
- 설치된 `MELEE`, `RANGED`, `DOT`, `SLOW` 타워는 실제 불투명 스프라이트의 발끝을 각 층 발판 접지선에 맞춘다. `STUN` 타워는 공중부양형 외형을 유지하도록 같은 접지선에서 36px 위에 표시하며 정지·공격 프레임 모두 같은 높이를 사용한다.
- UI 기능색은 금색(진행·재화), 청록색(상점 갱신·가능 상태), 붉은색(위험), 보라색(기본 판넬)으로 구분한다.
- 몬스터가 사망하면 실제 `rewardGold`를 지급하면서 사망 위치에 작은 금화가 위로 튀어 오르며 사라지는 연출을 표시하고, 상점 골드 제어부에 `+ n G` 획득 문구를 짧게 표시한다.
- 보유 골드는 골드 캡슐 정중앙에 고정한다. `+ n G`는 별도 하단 줄이나 판넬 경계에 종속하지 않고 보유 골드와 같은 수평 중심에서 나타나 캡슐 위쪽 바깥까지 상승하며 사라진다. 상점 카드의 동전 아이콘과 구매 가격은 실제 합산 폭을 기준으로 가격 캡슐 중앙에 정렬하고, `WAVE` 제목과 현재/전체 숫자는 중앙 원형 배지 안에서 각각 분리해 수평·수직 중앙을 맞춘다.
- 기본 UI 글꼴은 둥글고 굵은 한글 표시를 지원하는 `Jua`를 사용하며, 폰트 파일과 SIL Open Font License 1.1 원문을 저장소에 함께 보관한다.
- 타이틀 화면은 그래픽 디자이너가 제공할 타이틀 일러스트를 위한 빈 편집 영역과 하단 중앙의 `게임 시작`, `옵션`, `게임 크레딧` 버튼만 기본 화면에 표시한다. `게임 시작`은 일반 모드로 진입하고, 타이틀 전용 옵션 화면에는 테스트 환경 진입 기능을 두지 않는다. 실제 설정 항목이 확정되기 전까지 옵션 화면은 안내와 `돌아가기`만 제공한다.
- `게임 크레딧`은 별도 대형 스크롤 UI에서 현재 런타임 리소스의 제공자·제작 도구·공식 출처·라이선스를 표시한다. 세부 출처나 배포 이용 조건이 제공되지 않은 사용자 제공 리소스는 확인되지 않은 내용을 임의로 보완하지 않고 `세부 원출처·배포 라이선스 미제공, 공개·제출 전 팀 확인 필요`로 명시한다.
- 타이틀 배치와 일러스트 교체 지점은 `game/scenes/title_screen.tscn`, 크레딧 패널의 정적 배치와 문구는 `game/scenes/ui/title_credits.tscn`에서 편집한다. 자동 검증용 `?test_mode=1`, `--test-mode`, 자동 테스트·타워 시각 검사 인자는 플레이어 UI에 노출하지 않고 타이틀을 우회할 수 있다.
- 상점은 화면 하단에 배치한다.
- 상점은 TFT형 한 줄 배치를 사용한다. 왼쪽 전용 제어 영역에 골드와 리롤을 세로로 묶고, 오른쪽에 동일 크기의 터렛 카드 5장을 가로로 정렬한다.
- 일반 플레이 HUD와 상점 배치는 `game/scenes/ui/game_hud.tscn`에서 관리한다. 상단 HUD·진행/배속/일시정지 버튼·골드/리롤·카드 5장의 위치와 크기는 Godot 2D 편집기에서 직접 조절할 수 있어야 하며, 런타임 스크립트는 데이터와 동작만 연결한다.
- 상점 카드 내부의 프레임·터렛 이미지·이름·가격 영역·구매 완료·호버 상세 배치는 `game/scenes/ui/shop_card.tscn`에서 관리한다. 상점 카드는 `assets/ui/images/frame_1.png`, 상점 제어부는 `frame_2.png`, 버튼·금화·딤드는 같은 폴더의 공통 이미지를 9-패치 또는 개별 이미지로 조합하며 코드로 별도 카드 테두리나 금화를 그리지 않는다.
- 일시정지 옵션, 게임 클리어, 게임 오버, 테스트 옵션·데이터 편집 모달은 각각 `options_menu.tscn`, `game_clear_overlay.tscn`, `game_over_overlay.tscn`, `test_balance_panel.tscn`에서 관리한다. 정적 UI 노드를 GDScript의 `new()`로 생성하지 않는다.
- 데이터 편집 표의 가변 행·열은 런타임 데이터에 맞춰 생성하되 그리드·머리글·수정 셀·읽기 전용 셀은 각각 `balance_table_grid.tscn`, `balance_header_cell.tscn`, `balance_edit_cell.tscn`, `balance_readonly_cell.tscn` 템플릿을 인스턴스한다.
- 타워 설치 위치 표시는 `scenes/entities/tower_slot.tscn`, 몬스터 체력 바는 `scenes/entities/monster.tscn`에서 관리해 크기·위치·스타일을 Godot 편집기에서 직접 조절한다.
- 상단과 하단을 가로지르는 전체 폭 UI 배경 판넬은 사용하지 않는다. 상점의 골드·리롤 제어부와 카드 5장은 각각의 독립 판넬만 유지한다.
- 좌측 상단 HUD는 `assets/ui/images/top_1.png`·`top_2.png`의 반원형 낮/밤 프레임과 중앙 원형 정보 배지로 구성한다. 낮에는 중앙 배지에 남은 낮 시간만 표시하고, 밤에는 `WAVE 현재/전체` 정보만 표시한다. 낮·밤 풍경은 프레임 이미지 자체에 포함하며 두 이미지를 배경 전환과 함께 교차한다.
- 상단 HUD에는 낮의 밤 시작 버튼, 밤의 단일 배속 버튼과 항상 표시되는 일시정지 버튼만 배치하며, 전투 진행·임시 알림과 `최심부 코어 침입` 문구는 표시하지 않는다.
- 낮의 밤 시작 버튼은 낮에만 표시한다. 승리 후에는 게임 클리어 화면의 `다시하기` 버튼으로 새 플레이를 시작할 수 있고, 프로토타입 초기화와 게임 종료는 `ESC` 옵션 창에서도 실행할 수 있다.
- 패배하면 전장을 어둡게 덮는 전체 화면 딤드 위에 현재 게임 아트 스타일로 생성한 `GAME OVER` 일러스트 타이틀을 표시한다. 타이틀은 런타임 텍스트가 아니라 문자 형태가 그려진 투명 이미지이며, 기존 패배 상태 문구는 중복 표시하지 않는다.
- 게임 오버 화면에는 `다시 시작`과 `게임 종료` 버튼을 표시한다. `다시 시작`은 전체 플레이 상태를 초기화해 새 낮 단계에서 재개하고, `게임 종료`는 실행 중인 게임을 종료한다.
- 승리하면 전장을 따뜻한 갈색 계열의 전체 화면 딤드로 덮고, 밝은 샴페인 골드 계열로 생성한 `GAME CLEAR` 일러스트 타이틀을 표시한다. 타이틀은 런타임 텍스트가 아니라 문자 형태가 그려진 투명 이미지이며 기존 승리 상태 문구는 중복 표시하지 않는다.
- 게임 클리어 화면에는 타이틀 바로 아래 중앙에 `다시하기` 버튼 하나를 표시한다. 버튼을 누르면 전체 플레이 상태를 초기화해 새 낮 단계에서 재개하며, 프로토타입 초기화와 게임 종료는 `ESC` 옵션 창에서도 실행할 수 있다.
- `ESC`를 누르거나 우측 상단의 원형 일시정지 버튼을 누르면 게임의 타이머·몬스터·터렛·투사체·전장 연출을 모두 일시 정지하고 화면 중앙에 `계속하기`, `테스트 환경 시작`, `프로토타입 초기화`, `게임 종료`를 포함한 동일한 옵션 창을 표시한다. 옵션 창에서 `ESC`를 다시 누르거나 `계속하기`를 선택하면 게임을 재개한다. 일시정지 버튼은 낮과 밤 모두 항상 표시한다.
- 플레이 판단에 필요하지 않은 장식 텍스트와 몬스터 진행 방향 화살표는 표시하지 않는다.
- 밤에는 우측 상단에 파란 캡슐 모양의 게임 속도 버튼을 추가로 표시한다. 일반 배속 `1배`, `2배`, `3배`는 70% 크기의 `icon3.png` 진행 픽토그램을 각각 1개, 2개, 3개 표시하고 숫자는 함께 표시하지 않는다. 테스트 환경의 `5배`, `10배`처럼 3배속을 초과할 때만 `×5`, `×10` 숫자를 표시한다. 내부 일반 배속은 `1배 → 2배 → 3배 → 1배`로 적용하며, 낮·승리·패배 단계에서는 배속 캡슐을 숨기고 기본 속도 `1배`로 되돌린다.
- 테스트 환경은 일반 모드와 같은 데이터·시작 골드로 시작하고 화면에 `TEST` 배지를 표시한다. 상점에는 Tier 1 터렛 5종을 중복 없이 항상 한 장씩 표시하며, 기존 테스트 전용 배속은 유지해 밤에 `1배 → 3배 → 5배 → 10배 → 1배`로 순환한다.
- 테스트 환경에서는 화면 오른쪽에 배경만 약 30% 불투명한 별도 테스트 옵션 패널을 표시한다. 글자·입력칸·버튼은 가독성을 위해 완전 불투명하게 표시한다.
- 테스트 옵션 패널은 `+9999 GOLD`, 시작할 웨이브 선택·실행과 데이터 테이블 편집 창 열기 기능을 제공한다. 테스트 환경에서는 낮 준비 시간이 끝나도 밤을 자동 시작하지 않고, 밤의 기준 시간이 끝나도 다음 웨이브로 자동 이동하지 않는다. 모든 웨이브 시작·전환은 상단 시작 버튼 또는 테스트 패널의 웨이브 실행 버튼으로만 진행한다.
- 데이터 편집은 오른쪽 패널 안에 모든 필드를 나열하지 않고 `Define`, `Turret`, `Monster`, `SpawnTable`, `ShopGacha` 버튼으로 별도 대형 편집 창을 연다. 각 창은 행과 컬럼이 많은 데이터를 스크롤해 편집할 수 있어야 한다.
- 데이터 테이블 편집 창은 전체 화면 모달로 동작한다. 창이 열린 동안에는 편집 필드와 적용·원본 복원·닫기 버튼만 입력을 받고, 전장·상점·HUD·테스트 사이드 패널·ESC를 포함한 나머지 입력은 모두 무시한다. 편집 창은 전용 `닫기` 버튼으로 종료한다.
- 데이터 편집 셀의 값이 최초 로드한 로컬 JSON 원본과 다르면 금색 계열 배경·테두리로 강조한다. 적용 전 입력 중인 값과 이미 런타임에 적용된 값 모두 비교하며, 마우스를 올리면 원본값을 표시하고 원본 복원 시 강조를 제거한다.
- 테스트 데이터 편집은 런타임 복사본에만 적용하며 Google Sheets와 로컬 JSON 원본을 수정하지 않는다. 수치·확률·불리언 등 밸런스 필드는 편집할 수 있고 ID·리소스 참조 필드는 읽기 전용으로 표시한다. 원본값 초기화로 현재 세션의 변경을 폐기할 수 있다.
- 테스트 데이터 편집 창은 행 추가와 행 삭제를 지원한다. 기존 행의 ID·리소스 참조는 계속 읽기 전용이지만 새 행은 적용 전까지 ID·참조 필드를 입력할 수 있으며, 적용 후에는 기존 행과 같은 읽기 전용 상태가 된다. 참조 중인 행 삭제, 중복 ID, 잘못된 참조와 전체 데이터 무결성 오류는 적용하지 않고 편집 상태를 유지한다.
- 테스트 데이터 편집 창의 파싱 입력란은 현재 테이블의 CSV·TSV·JSON 행을 읽어 미적용 신규 행으로 추가한다. 헤더 기반 CSV·TSV와 JSON 객체 한 개 또는 객체 배열을 지원하고, 파싱·타입 변환·필수 컬럼 오류를 결과 영역에 표시한다.
- 터렛 데이터 변경은 설치된 터렛에도 갱신하고, 몬스터 최대 체력 변경은 현재 체력 비율을 유지해 기존 몬스터에 반영한다. SpawnTable 변경은 다음 스폰부터 사용한다.
- 사람은 일시정지 메뉴에서 테스트 환경을 켜거나 일반 모드로 돌아갈 수 있다. Web 빌드는 URL 쿼리 `?test_mode=1`, Godot 실행은 사용자 인자 `--test-mode`로 테스트 환경에 직접 진입할 수 있다.
- 낮에는 밝은 하늘, 밤에는 어두운 하늘과 화면 색조가 보이도록 두 단계 사이를 짧게 전환한다.
- 첫 화면에는 브라우저 오디오 제한에 대응하는 사용자 클릭 시작 버튼을 둔다.

## 8. 데이터 기획과 플레이스홀더

게임 데이터는 `Define`, `Turret`, `Monster`, `SpawnTable`, `ShopGacha` 구조로 분리한다.

- `Define`: `prepareTimeSec`, `totalWaveCount`, `waveTimeSec`, `initialGold`, `rerollCost`, `rerollPlusCost`, `failAllowedMonster`, `monsterSpawnInterval`, `spawnOrderInterval`
- `Turret`: 원본 식별자, 유형, 다음 Tier, 상점 여부, 가격, 공격력, 공격 주기, 사거리, 범위값, CC 값, VFX, 외형 리소스
- `Monster`: 원본 식별자, 유형, 체력, 이동 속도, 보상 골드, 프리팹 리소스
- `SpawnTable`: 웨이브별 몬스터 스폰 순서
- `ShopGacha`: Tier 1 터렛의 상점 등장 확률
- 프로토타입 확장 필드: 표시 이름, 도형 색상, 터렛 Tier

초기 자료의 구조 검증용 값은 다음과 같다.

- 준비 시간: 20초
- 전체 웨이브 수: 4
- 웨이브 전투 시간: 80초
- 초기 골드: 100
- 상점 기본 리롤 비용: 10
- 몬스터 스폰 간격: 0.4초
- 서로 다른 Spawn Order 그룹 사이의 간격: 0.5초
- 몬스터 유형: `NORMAL`, `SPEED`, `TANK`, `BOSS`
- 보스 CC 적용률: 일반 효과의 50%
- Tier 1 터렛 5종의 초기 상점 등장 확률: 각 0.2
- 각 웨이브는 최신 데이터 테이블의 `spawnOrder` 오름차순으로 실행하며, 같은 `spawnOrder`의 행은 함께 생성한다.
- `wave4`는 2026-08-08 최신 데이터에 따라 `normal2`, `spped2`, `tank2`를 각각 300기 생성한다.

위 값은 최종 밸런스가 아니다.

## 9. AI 공세 디렉터 후보

- 인게임 AI는 공모전 필수 기능이 아니라 차별화 후보 기능이다.
- 우선 후보는 외부 API가 없는 유틸리티 기반 공세 디렉터다.
- AI는 웨이브 시작 전에만 판단하고 검증된 공세 템플릿 중 하나를 선택한다.
- 입력 후보: 층별 DPS, 공격 유형 비율, CC 비율, 평균 Tier, 적의 층별 잔여 HP, 누수 수, 코어 상태
- 출력 후보: `NORMAL`, `SPEED`, `TANK`, 혼합, 보스 공세 템플릿
- 동일 카운터 연속 선택 금지, 난이도 상승 상한, 낮은 체력 시 완화와 안전한 기본 공세 폴백을 둔다.
- 플레이어에게 다음 공세와 판단 근거를 사전 예고한다.

## 10. 미확정 및 검증 필요 항목

- `20260804 REQ-DESIGN-001` 답변의 “다른 터렛”이 서로 다른 `turretId` 머지까지 뜻하는지와 조합 결과 규칙 (`Design Request/20260805 REQUEST.md`의 `REQ-DESIGN-007`)
- 화면 스케치의 상점 카드 4개와 터렛 5종 데이터의 관계
- 최종 웨이브 수와 보스 구성
- 데이터 기반 상점 카드 수 자동 배치와 화면 폭 초과 처리 방식 (`Design Request/20260805 REQUEST.md`의 `REQ-UI-001`)
