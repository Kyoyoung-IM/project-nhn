# 게임 명세

이 문서는 NAN 2026 머지 디펜스의 확정 규칙, 임시 데이터, 불일치 및 검증 필요 사항을 관리한다.

## 문서 원칙

- 사용자 답변으로 확정된 규칙에는 확인 날짜를 기록한다.
- 초기 PDF, 시트, 화면 스케치는 참고자료이며 서로 충돌할 경우 임의로 우선순위를 정하지 않는다.
- 미확정 값은 코드와 데이터에서 `TODO`, `PLACEHOLDER` 또는 개발 플래그로 표시한다.
- 밸런스 값은 교체 가능한 데이터로 관리하고 전투 코드에 직접 하드코딩하지 않는다.

## 현재 확정된 방향

- 2026-08-04 기획자 확정: 몬스터는 공격하지 않는다. 몬스터는 경로 이동과 최심부 도달 판정만 수행하고 터렛 체력·사망 시스템은 사용하지 않는다.
- 2026-08-04 기획 변경 확정: 보스 한 기 처치 시 즉시 승리하지 않는다. 마지막 보스 웨이브의 모든 몬스터가 생성되고 이전 웨이브 잔존 적을 포함한 전장의 모든 적을 처치해야 승리한다.
- 2026-08-04 기획 변경 확정: 각 웨이브는 데이터의 `waveTimeSec=50초`를 기준으로 진행하고, 마지막 웨이브 전까지 시간이 끝나면 기존 적과 진행 중 투사체를 제거하지 않은 채 낮 단계 없이 다음 웨이브를 즉시 추가로 시작한다. 마지막 웨이브에는 다음 웨이브가 없으므로 모든 적 처치 또는 최심부 도달 판정까지 현재 밤을 유지한다.
- 2026-08-04 기획자 확정: `MELEE`는 할퀴기 피격 이펙트가 있는 히트스캔, `DOT`는 화염방사 투사체와 화상, `SLOW`는 눈덩이 투사체와 둔화, `RANGED`는 초록색 콩알 투사체, `STUN`은 전기 충전 뒤 히트스캔 낙뢰와 헤롱헤롱 기절 이펙트를 사용한다.
- 2026-08-04 기획자 확정: 투사체 공격은 명중 시 기존 피해·상태이상 규칙을 적용하며 `DOT` 공격 사거리는 `rangeValue=1.5칸`을 사용한다. 나머지 비근접 타입은 기준 테이블의 `range=2칸`을 유지한다.
- Godot 4와 GDScript를 사용한다.
- 브라우저에서 실행되는 Web 빌드를 목표로 한다.
- Compatibility 렌더러와 단일 스레드 Web 내보내기를 기본으로 한다.
- 적은 고정 수평 경로와 상태 전환을 이용해 층별로 이동한다.
- 제출 빌드는 외부 데이터 서비스 없이 로컬 데이터만으로 동작해야 한다.
- 2026-08-03 확인: 지상층은 몬스터의 지하 진입 연출을 위한 비전투 공간이다.
- 2026-08-03 확인: 실제 전투는 지하 3개 층에서 진행한다.
- 2026-08-03 확인: 적은 지상에서 진입한 뒤 지하 3개 전투층 모두에서 오른쪽에서 왼쪽으로 진행한다.
- 2026-08-03 확인: 각 전투층의 왼쪽 출구에 도달하면 다음 층의 오른쪽 입구로 하강 전환한다.
- 2026-08-03 확인: 몬스터가 가장 깊은 층의 경로 끝에 도달하면 플레이어가 패배한다.
- 2026-08-03 확인: 최심부 코어는 지하 3층의 왼쪽 경로 끝에 배치한다.
- 지상층은 향후 기능 확장을 고려해 전투층과 분리된 구간으로 유지한다.
- 2026-08-03 확인: 각 지하 전투층은 고정 터렛 슬롯 5개를 제공한다.
- 2026-08-08 전장 배치 변경: 중앙 50%에 균등 배치했던 각 전투층 슬롯 5개를 화면 너비의 20%인 `384px`만큼 왼쪽으로 평행 이동해 화면 X `96/336/576/816/1056`에 배치한다. 슬롯 간격 `240px`, 몬스터 속도와 터렛 사거리 데이터는 변경하지 않는다.
- 2026-08-04 분기 변경: `agent/indestructible-towers-day-night` 버전의 아군 터렛은 체력과 사망 상태가 없는 고정 불파괴 오브젝트다.
- 2026-08-04 확인: 같은 층의 터렛과 몬스터 간 사거리는 화면 배치의 수직 간격을 제외하고 수평 거리만 비교한다.
- 2026-08-04 분기 변경: 원본 Monster 테이블에 공격 관련 속성이 없으므로 몬스터의 터렛 공격·저지 로직을 사용하지 않는다.
- 2026-08-03 확인: 화면 하단에는 TFT 챔피언 상점 형태의 터렛 구매 UI를 배치한다.
- 2026-08-03 확인: 상점 카드는 기본 상태에서 터렛 이미지·이름·가격을 표시하고, 마우스 호버 시 이름·이미지·효과·상세 능력치를 표시한다.
- 2026-08-03 확인: 골드 표시와 리롤 버튼은 상점에 속한 기능이므로 상점 카드 옆의 전용 제어 영역에 배치한다.
- 2026-08-03 확인: 일반 플레이의 상점 카드는 `ShopGacha` 확률에 따라 실행·초기화·리롤마다 무작위로 추첨한다. 자동 테스트와 디버그 실행은 재현 가능한 고정 시드를 지원한다.
- 2026-08-03 확인: 터렛 구매는 상점 카드에서 빈 슬롯으로 드래그하는 방식으로 진행하며, 유효한 빈 슬롯에 드롭해 설치가 확정된 뒤에만 골드를 차감한다.
- 2026-08-03 확인: 상점 카드를 잘못된 위치나 점유된 슬롯에 놓으면 설치와 골드 차감을 모두 취소한다.
- 2026-08-04 분기 용어 변경: 상점 카드 구매·빈 슬롯 배치·리롤은 낮과 밤 모두 가능하다.
- 2026-08-05 기획자 확정: 설치된 터렛의 빈 슬롯 이동은 낮·같은 층에서만 가능하지만, 동일 `turretId`·Tier 설치 터렛 간 수동 머지는 낮과 밤 모두 가능하며 서로 다른 층도 허용한다.
- 2026-08-03 확인: 리롤 버튼은 사용 가능 상태를 밝은 강조색으로, 골드 부족 상태를 어두운 회색과 명시적인 부족 문구로 구분한다.
- 2026-08-04 분기 용어 변경: 낮에는 설치된 터렛을 마우스로 드래그해 같은 층의 빈 슬롯으로 이동할 수 있다.
- 2026-08-04 분기 용어 변경: 설치된 터렛은 다른 층으로 드래그 이동할 수 없으며 밤에는 이동할 수 없다.
- 2026-08-05 기획자 확정: 낮·밤에 관계없이 동일 `turretId`·Tier 터렛을 직접 드래그해 같은 층 또는 다른 층의 호환 터렛에 겹치면 두 터렛을 `nextTurretId`의 상위 터렛 하나로 머지한다. 결과는 드롭 대상 슬롯과 층에 남고 자동 머지는 하지 않는다.
- 2026-08-05 구현 구분: 밤의 설치 터렛 드래그는 머지에만 사용한다. 밤의 빈 슬롯 이동과 낮의 층간 빈 슬롯 이동은 허용하지 않는다.
- 2026-08-04 그래픽 확정: 머지 결과의 전투 스탯은 데이터 테이블의 상위 터렛 행을 사용하고 외형은 5종 각각의 Tier 1~4 전용 스프라이트로 교체한다. Tier가 높아질수록 표시 크기가 조금씩 증가하며 짧은 승급 링·빛 점 이펙트를 유지한다.
- 2026-08-04 그래픽 확정: `DOT` 모든 Tier의 불꽃과 `STUN` Tier 4의 번개는 본체 이미지와 분리된 독립 이펙트 텍스처·노드로 표시한다. `DOT` Tier 3·4는 후방 본체, 불꽃, 전방 본체의 3단 레이어로 나눠 불꽃이 몸체 안에서 솟는 깊이를 표현한다.
- 2026-08-04 그래픽 보정: 5종 전체의 Tier별 표시 크기는 투명 캔버스가 아닌 실제 불투명 그림 면적으로 정규화해 Tier 1~4 순서대로 일정하게 증가시킨다. `DOT` 불꽃과 `STUN` 번개도 실제 그림 경계를 기준으로 본체에 정렬하며, 낮고 넓은 `DOT` Tier 4에는 Tier 3보다 확실히 커 보이는 별도 본체·불꽃 확대값을 적용한다.
- 2026-08-04 그래픽 변경: 구름 형태의 `STUN` 타워는 발판 기준 96px 위에서 미세하게 상하 부유한다. 이전 144px 높이의 2/3이며 Tier 4 번개도 본체 이동을 따른다.
- 2026-08-04 상점 그래픽 확정: 기본 카드의 유색 이미지 배경 판넬을 제거하고 투명 배경의 Tier 1 타워 스프라이트만 표시한다. 이름·가격과 기존 호버 상세 정보 구조는 유지한다.
- 2026-08-04 확인: 상점 카드를 동일 `turretId`·Tier의 설치 터렛에 드롭하면 낮과 밤 모두 카드 가격을 지불하고 상위 Tier로 머지한다. 호환되지 않는 점유 슬롯에서는 구매와 결제를 취소한다.
- 2026-08-07 기획 변경 확정: 설치 터렛 판매와 골드 환급을 제공하지 않는다. 설치 터렛을 유효한 이동·머지 대상이 아닌 곳에 드롭하면 원래 슬롯으로 복귀하며 골드는 변하지 않는다.
- 2026-08-04 데이터 갱신: `totalWaveCount=4`, `rerollPlusCost=5`, `spawnOrderInterval=0.5`가 기준 테이블의 정식 값으로 제공되어 기존 로컬 보정을 제거했다.
- 2026-08-04 데이터 갱신: `wave3`의 `spawnOrder`가 1~10의 고유 값으로 정리되어 모든 웨이브를 `spawnOrder` 오름차순으로 실행한다.
- 2026-08-04 분기 용어 변경: 게임 시작 전과 각 밤 시작 전에는 `prepareTimeSec` 동안 낮 단계를 둔다.
- 2026-08-04 갱신: 리롤 비용은 최초 낮부터 실행할 때마다 `rerollPlusCost`만큼 누적 증가하고, 웨이브 사이 낮 단계가 없으므로 게임 초기화 때 기본 비용으로 초기화한다.
- 2026-08-03 확인: 기준 데이터 테이블에 `rerollPlusCost`가 없어 기획 확인 전까지 로컬 프로토타입은 기존 임시값 `5`를 유지한다.
- 2026-08-03 확인: `totalWaveCount=5`이지만 `wave5` 스폰 데이터가 없어 기획 확인 전까지 로컬 프로토타입을 4웨이브로 구성한다.
- 2026-08-03 확인: `wave3`의 중복 `spawnOrder`는 병합하거나 재정렬하지 않고 데이터 테이블 행 순서대로 실행한다.
- 2026-08-03 확인: 자료에는 개체 간 스폰 간격 `monsterSpawnInterval=0.4초`만 있고 서로 다른 Spawn Order 그룹 간격은 없으므로, 기획 확정 전까지 로컬 프로토타입에 `spawnOrderIntervalSec=10초`를 적용한다.
- 2026-08-03 확인: `failAllowedMonster`는 원본 데이터 호환 필드로 보존하지만 현재 패배 판정에는 사용하지 않는다. 패배 조건은 최심부 코어 도달 규칙을 유지한다.
- 2026-08-03 확인: 오브젝트 데이터는 `Define`, `Turret`, `Monster`, `SpawnTable`, `ShopGacha` 구조로 분리한다.
- 2026-08-03 확인: Web 게임 화면은 브라우저 크기 변화에 따라 기준 해상도 `1920×1080`의 비율을 유지하며 자동 확대·축소하고, 남는 축의 가운데에 배치한다.
- 2026-08-03 확인: 플레이 판단에 필요하지 않은 장식 텍스트와 몬스터 진행 방향 화살표는 표시하지 않는다.
- 2026-08-04 확인: 웨이브 진행 중에는 `×1`, `×2`, `×3` 게임 속도를 선택할 수 있으며, 웨이브가 끝나면 기본 속도 `×1`로 초기화한다.
- 2026-08-04 분기 변경: 준비 단계의 표시 명칭은 `낮`, 웨이브 단계의 표시 명칭은 `밤`이며 두 단계 사이에 짧은 색조 전환 연출을 사용한다.
- 2026-08-04 그래픽 분기: `agent/casual-rpg-background`에서 AI 생성 캐주얼 판타지 광산 배경을 사용한다. 지상·지하 3층·하단 창고의 미적 구성을 우선하고 경로와 슬롯 좌표를 배경 발판에 맞춘다.
- 2026-08-04 그래픽 분기: 이번 단계에서는 배경만 생성·교체하고 터렛·몬스터·상점 카드 등 기존 더미 오브젝트 외형은 유지한다.
- 2026-08-04 그래픽 분기: 낮 배경의 전체 구도를 보존한 밤 배경을 추가하고, 단계 전환 시 두 텍스처를 1.2초 동안 교차 페이드한다.
- 2026-08-04 그래픽 보정: 별도 생성된 밤 원본의 플랫폼이 낮 원본보다 위로 이동한 편차를 렌더링 단계의 세로 배율·오프셋으로 보정해 낮/밤 전환 중 지형 기준선을 일치시킨다.
- 2026-08-05 그래픽 변경: v7 낮/밤 배경은 세 전투 플랫폼의 상단을 원본 픽셀 행 `848/1081/1320`에 맞췄다. 이전 v6용 밤 배경 확대·이동 보정은 제거하고 두 배경을 동일 좌표·동일 배율로 렌더링한다.
- 2026-08-05 그래픽 변경: 지하의 작은 황금 광석 군집은 B1~B3에 각각 2개·3개·1개를 비대칭 배치하고, 각 군집의 결정 수·크기·기울기와 종유석 길이·간격을 불규칙하게 구성한다.
- 2026-08-05 그래픽 변경: `RANGED` 콩알, `DOT` 화염, `SLOW` 눈덩이 투사체와 `MELEE` 할퀴기, `STUN` 충전·낙뢰·헤롱헤롱 효과를 캐주얼 RPG 스타일의 독립 투명 PNG로 교체한다.
- 2026-08-05 기획 변경 확정: `DOT`는 일반 투사체 방식에서 분리해 발사구와 대상 사이를 짧게 잇는 화염방사 공격으로 변경한다. 화염이 대상에 닿는 시점에 기본 피해와 화상을 적용하며 기존 `rangeValue=1.5칸`을 유지한다.
- 2026-08-08 화염 표시 확정: `DOT` 화염 PNG의 원래 좌→우 방향을 유지해 좁은 쪽을 포구에 둔다. 이 이펙트에 한해 원본 종횡비를 보존하지 않고 가로로 늘리며, 넓은 쪽 불투명 영역이 생성 즉시 대상의 실제 이미지 중심을 지나 48px 겹치도록 공격 표시 동안 연결한다. 투명 캔버스 여백은 표시 길이 계산에서 제외한다.
- 2026-08-05 전투 규칙 변경: 발사된 투사체와 진행 중인 화염방사는 발사 당시 전투층에만 유효하다. 대상이 층간 하강을 시작하거나 다른 층으로 이동하면 추적을 중단하고 피해 없이 소멸한다.
- 2026-08-05 그래픽 변경: `SLOW`와 `RANGED` 투사체 이미지의 고정 트레일은 제거하고 실제 이동 궤적을 따라 런타임 트레일을 생성한다. `DOT`는 길이·방향이 변하는 별도 화염방사 연출을 사용한다. 낙뢰 위에는 작은 무표정 먹구름과 지면 접촉 섬광을 배치하고, STUN 충전은 얇고 은은한 전기 고리로 표현한다. 전투 이펙트 최종 표시 크기는 직전 버전보다 50% 확대한다.
- 2026-08-05 전투 표시 변경: 몬스터는 설치 터렛보다 앞쪽 레이어에 표시한다. 몬스터 체력 바는 첫 유효 피해 전까지 숨기고 피해를 받은 뒤 실제 그림의 머리 위에 얇은 막대로 표시한다.
- 2026-08-05 배경 변경: v8 낮 배경에는 산 능선 부근에 해를 추가하고, 밤 배경의 달도 같은 높이로 내려 지상 카메라에서 잘리지 않게 한다. 낮·밤은 동일 크기와 동일 렌더 좌표를 유지한다.
- 2026-08-08 몬스터 크기 변경: `normal1`의 현재 표시 배율을 보스를 제외한 `normal1`, `spped1`, `tank1`에 공통 적용해 각 스프라이트의 실제 불투명 그림 픽셀 크기 차이를 그대로 표시한다. 보스는 기존 440px 높이를 유지한다. 모든 타입은 종횡비와 발판 접지를 유지하며 피격·사망·스폰·층 이동 연출에도 같은 타입별 배율을 적용한다.
- 2026-08-07 몬스터 피격 표시 변경: 몬스터가 직접 피해를 받으면 `game/assets/enemy/hit animation/`의 타입별 피격 이미지로 0.25초 전환한다. 표시 중 직접 피해를 다시 받으면 남은 시간에 0.1초를 가산하며, 화상 주기 피해는 표시와 가산 조건에서 제외한다.
- 2026-08-07 몬스터 사망 표시 변경: 몬스터 체력이 0이 되면 `game/assets/enemy/death animation/`의 타입별 5프레임 세로 시트를 0.6초 동안 재생한 뒤 노드를 제거한다. 사망 즉시 이동·공격 대상·상태이상 판정에서는 제외하고 처치 보상과 웨이브 판정은 즉시 처리한다.
- 2026-08-07 배경 변경: 현재 낮·밤 배경을 각각 `game/assets/backgrounds/bg.png`, `game/assets/backgrounds/bg_night.png`로 교체한다. 두 이미지는 동일한 `887×1774` 구도를 같은 좌표와 균일 배율로 렌더링하며 기존 1.2초 교차 페이드와 카메라 클리핑을 유지한다.
- 2026-08-08 전장 좌표 변경: 새 배경의 지상·B1·B2·B3 접지선을 각각 월드 Y `1336/1862/2398/2916`으로 맞춘다. 고정 전체 뷰의 몬스터는 타입별 실제 발끝을 접지선에 고정하며 각 층의 화면 밖 오른쪽 화면 X `1980`에서 진입해 화면 밖 왼쪽 화면 X `-60`으로 퇴장한다. 편집 씬의 대응 월드 X는 `3717/-1797`이며, B3 패배 판정도 왼쪽 맵 밖에 도달한 뒤 발생한다.
- 2026-08-08 타워 접지 변경: B1~B3 슬롯 기준 Y를 각 접지선보다 100px 위에 배치하고 `MELEE`, `RANGED`, `DOT`, `SLOW`의 실제 발끝을 발판에 맞춘다. 공중부양형 `STUN`은 정지·공격 프레임 모두 접지선보다 36px 위에 표시한다.
- 2026-08-08 타워 애니메이션 확정: 설치된 아군 타워는 타입·Tier별 2프레임 대기 시트를 반복하고, 공격 시 타입·Tier별 2×2 시트를 좌상→우상→좌하→우하 순서로 한 번 재생한 뒤 대기로 복귀한다. Web은 시트를 표시 크기에 맞춰 축소 임포트하고 실제 배치된 타입·Tier만 지연 로드한다.
- 2026-08-04 그래픽 분기: 낮·밤 배경의 구도와 게임 좌표는 유지하면서 미세 질감과 명암 단계를 줄인 캐주얼 모바일 RPG형 단순화 버전으로 교체한다.
- 2026-08-04 사용자 확정: 세로형 v6 배경을 화면 폭 기준의 균일 배율로 확대하고 카메라로 세로 구간을 클리핑한다. 장식 가장자리는 잘릴 수 있지만 B1~B3 플랫폼 전체는 안전 영역 안에 유지한다.
- 2026-08-04 사용자 확정: 지상 광산 입구와 오른쪽 숲 앞의 흙길을 제거하고, 몬스터는 오른쪽 숲에서 생성되어 높낮이 변화 없는 수평 직선으로 안쪽 광산 입구까지 이동한다.
- 2026-08-04 사용자 확정: 지하 3개 조명은 단일 기둥형 매달림 랜턴으로 통일하며 같은 가로 위치에 정렬한다.
- 2026-08-04 사용자 확정: 바닥형 대형 황금 광물과 고정식 자동 드릴 에셋은 향후 기믹 후보로 보관하되, 당장은 전장에 배치하지 않는다.
- 2026-08-04 사용자 확정: 지상과 지하 몬스터 이동 높이는 종류별 도형의 실제 바닥이 지면 또는 각 플랫폼 두께의 중앙선에 닿도록 맞춘다. 기존 중심 경로보다 접촉선을 82px 내리고 도형별 하단 반경 12~22px를 적용한다.
- 2026-08-04 UI 분기: HUD·상점 카드·버튼·배치 슬롯을 굵은 짙은 외곽선, 둥근 모서리, 판넬 그림자와 금색·청록·붉은색·보라색 기능 팔레트로 통일한다.
- 2026-08-04 UI 분기: 공통 UI 글꼴을 SIL OFL 1.1의 무료 `Jua`로 변경하고 폰트 및 라이선스 원문을 로컬 빌드에 포함한다.
- 2026-08-04 사용자 확정: 배경은 기존 좌표와 층 구조를 유지하되 레퍼런스 수준의 단순한 실루엣과 약 3~4단계의 평평한 명암으로 대폭 단순화한다.
- 2026-08-08 사용자 변경 확정: 터렛 설치 위치 UI는 기존 표시의 가로·세로 크기와 드롭 판정 영역을 각각 2배로 확대한다. 평상시 빈 슬롯을 기본 불투명도 30%로 표시하고 포탑이 설치된 점유 슬롯은 0%로 숨긴다. 상점 카드 구매 또는 설치 타워 이동·머지 드래그 중 유효한 빈 슬롯과 머지 대상은 60%로 강조하고, 조작 종료 후 빈 슬롯은 30%, 점유 슬롯은 0%로 복원한다.
- 2026-08-04 사용자 확정: 밤의 게임 속도 UI는 단일 버튼으로 통합하고 클릭할 때마다 `1배 → 2배 → 3배 → 1배` 순환한다. 2026-08-08 변경 후 일반 1·2·3배속은 70% 크기의 진행 픽토그램을 각각 1·2·3개 표시하고 숫자를 함께 표시하지 않는다. 테스트 환경의 5·10배속처럼 3배속을 초과할 때만 숫자를 표시한다.
- 2026-08-04 작업 절차 확정: UI 위치 또는 구성 요소를 변경하기 전에는 간단한 예상 배치도를 제시하고 사용자 승인을 받은 뒤 구현한다.
- 2026-08-08 타이틀 화면 확정: 실행 시 그래픽 디자이너용 빈 일러스트 영역과 하단 중앙의 `게임 시작`, `옵션` 버튼만 있는 타이틀을 먼저 표시한다. `게임 시작`은 일반 모드로 진입하며 타이틀 전용 옵션에는 테스트 환경 진입 기능을 두지 않는다. 설정 항목 확정 전에는 안내와 `돌아가기`만 제공한다. 자동·직접 검증용 테스트 인자와 Web 쿼리는 타이틀 UI에 노출하지 않고 기존처럼 테스트 환경을 직접 호출할 수 있다.
- 2026-08-08 전장 뷰 변경 확정: `1920×1080` 한 화면에 지상과 B1~B3를 항상 함께 표시한다. 전장 카메라 정지점과 마우스 휠·위/아래 방향키 이동은 폐지하며, 배경·몬스터·터렛·전투 이펙트는 동일한 고정 균일 배율을 사용하고 HUD·상점은 기존 크기로 유지한다.
- 2026-08-04 패배 지점 표시 변경: B3 왼쪽 끝의 코어 그래픽은 제거하고, 몬스터가 해당 경로 끝 좌표에 도달하면 즉시 패배하는 판정만 유지한다.
- 2026-08-04 UI 배치 확정: 하단 상점은 왼쪽 골드·리롤 제어 영역과 오른쪽 동일 크기 카드 5장의 TFT형 한 줄 구성으로 재배치한다.
- 2026-08-04 UI 배치 확정: 상단 중앙의 전투 진행·임시 알림 영역은 제거하고 낮·밤, 웨이브, 낮의 밤 시작 버튼 또는 밤의 단일 배속 버튼만 유지한다.
- 2026-08-04 UI 배치 확정: `ESC` 옵션 창에서 게임을 일시 정지하고 계속하기·프로토타입 초기화·게임 종료를 제공한다. 2026-08-08 변경으로 게임 클리어 화면에는 `다시하기` 버튼을 다시 제공한다.
- 2026-08-04 HUD 표시 확정: 낮에는 중앙 HUD에 남은 낮 시간만, 밤에는 `WAVE 현재/전체` 정보만 표시한다.
- 2026-08-04 전투 제어 UI 확정: 낮에는 원형 일시정지 버튼만, 밤에는 현재 배속 수만큼 진행 삼각형을 표시하는 캡슐 버튼 및 원형 일시정지 버튼을 함께 표시한다. 테스트 환경에서 3배속을 초과할 때만 배속 숫자를 표시한다. 일시정지 버튼은 `ESC`와 동일한 옵션 창을 연다.
- 2026-08-04 테스트 환경 확정: 일시정지 메뉴에 `테스트 환경 시작`을 추가하고 진입 시 현재 플레이를 초기화한다. 테스트 환경은 `GOLD 99999`, 다섯 Tier 1 타워 고정 상점, `TEST` 배지, 밤 전용 `1·3·5·10배` 순환 배속을 제공하며 같은 메뉴에서 일반 모드로 복귀할 수 있다.
- 2026-08-04 테스트 진입 확정: 사람은 일시정지 메뉴로 접근하고, 자동·직접 검증은 Web URL의 `?test_mode=1` 또는 Godot 사용자 인자 `--test-mode`로 테스트 환경을 즉시 호출할 수 있다.
- 2026-08-05 테스트 환경 변경: 테스트 모드는 일반 모드와 같은 시작 골드와 무작위 상점으로 시작한다. 기존 테스트 전용 `1·3·5·10배` 순환 배속은 유지하고, `+9999 GOLD`와 특정 웨이브 시작은 오른쪽 테스트 옵션 패널에서 사용자가 직접 실행한다.
- 2026-08-05 테스트 데이터 편집 확정: 오른쪽 반투명 테스트 패널에서 `Define`, `Turret`, `Monster`, `SpawnTable`, `ShopGacha`별 대형 편집 창을 연다. 편집은 테스트 세션의 런타임 복사본에만 적용하고 Google Sheets와 로컬 JSON은 변경하지 않는다. ID·리소스 참조는 읽기 전용이며 원본값 초기화를 지원한다.
- 2026-08-05 테스트 데이터 편집 입력 확정: 대형 데이터 편집 창이 열리면 게임을 일시 정지하고 편집 창 내부와 전용 닫기 버튼만 입력을 받는다. 전장·상점·HUD·테스트 사이드 패널·ESC 등 편집 창 외부 입력은 닫기 전까지 무시한다.
- 2026-08-05 테스트 데이터 변경 표시 확정: 최초 로드한 로컬 JSON 원본과 다른 편집 셀은 금색 계열 배경·테두리로 강조하고 원본값을 툴팁으로 표시한다. 적용 전 변경과 런타임 적용 후 변경을 모두 유지해 표시하며 원본 복원 시 해제한다.
- 2026-08-06 테스트 데이터 행 편집 확정: 데이터 편집 모달에서 행 추가·삭제를 지원한다. 기존 행의 ID·리소스 참조는 읽기 전용으로 유지하고 신규 행은 적용 전까지만 해당 필드를 입력할 수 있다. 참조 중인 행 삭제, 중복 ID와 전체 데이터 무결성 오류는 적용하지 않는다.
- 2026-08-06 테스트 데이터 파싱 확정: 현재 테이블의 헤더 포함 CSV·TSV 또는 JSON 객체·객체 배열을 붙여넣어 신규 행으로 파싱할 수 있다. 파싱 결과는 먼저 미적용 행으로 표시하고 `변경 적용` 시에만 테스트 세션 런타임 복사본에 반영한다.
- 2026-08-05 테스트 환경 변경: 테스트 환경의 상점은 무작위 추첨 대신 Tier 1 터렛 5종을 중복 없이 항상 한 장씩 표시한다. 테스트 환경에서는 준비 시간과 웨이브 기준 시간이 끝나도 단계가 자동 진행되지 않으며, 상단 시작 버튼 또는 테스트 패널에서 선택한 웨이브 실행으로만 웨이브를 시작·전환한다. 기존 `1·3·5·10배` 순환 배속은 유지한다.
- 2026-08-05 UI 편집 구조 확정: 일반 HUD와 상점 전체 배치는 `game/scenes/ui/game_hud.tscn`, 카드 내부 배치는 `game/scenes/ui/shop_card.tscn`에서 Godot 2D 편집기로 직접 조절한다. GDScript는 데이터·입력·상태 갱신만 담당하며 `assets/ui/images`의 이미지 프레임 외에 중복 코드 테두리는 사용하지 않는다.
- 2026-08-08 전장 편집 구조 변경: 적 스폰·층간 이동·최종 패배 위치와 B1~B3 타워 슬롯 15개는 `game/scenes/battlefield_layout.tscn`의 편집 가능한 Marker2D로 유지하되, 폐지된 카메라 정지 위치 Marker2D는 제거한다. 이동 경로와 슬롯 Marker2D의 순서와 개수는 런타임 상태 인덱스를 위해 유지한다.
- 2026-08-05 게임 오버 화면 확정: 패배 시 전장을 전체 화면 딤드로 어둡게 하고, 런타임 텍스트 대신 게임 아트 스타일로 생성한 `GAME OVER` 일러스트 타이틀을 표시한다. 기존 패배 상태 문구는 제거하며 `다시 시작`과 `게임 종료` 버튼을 제공한다.
- 2026-08-08 게임 클리어 화면 변경 확정: 승리 시 전장을 따뜻한 갈색 계열 딤드로 덮고, `GAME OVER`와 같은 굵고 둥근 2단 입체 구조이되 밝은 샴페인 골드·백금색으로 구분한 `GAME CLEAR` 투명 일러스트 타이틀을 표시한다. 기존 승리 상태 문구는 제거하며 타이틀 아래 중앙의 `다시하기` 버튼으로 전체 플레이 상태를 초기화해 새 낮 단계에서 재개한다. 초기화와 종료는 기존 `ESC` 옵션 창에서도 제공한다.
- 2026-08-06 일시정지 동작 보완: `ESC` 또는 원형 일시정지 버튼으로 옵션 창을 열면 게임 타이머뿐 아니라 몬스터 이동, 터렛 공격, 투사체와 전장 연출을 모두 정지한다. 옵션 창에서 `ESC`를 다시 누르거나 `계속하기`를 선택하면 재개한다.
- 2026-08-05 UI 편집 구조 확대: 일시정지 옵션, 테스트 옵션·데이터 편집 모달, 데이터 표 셀 템플릿, 설치 슬롯과 몬스터 체력 바까지 독립 `.tscn`으로 분리한다. GDScript는 동적 문구·상태·데이터 반복만 갱신하며 모든 정적 위치·크기·폰트·색·스타일은 Godot 편집기에서 조절한다. 상세 경로는 `docs/UI_EDITING_GUIDE.md`를 따른다.
- 2026-08-04 상점 표시 확정: 밤에도 상점 카드 밝기와 색상을 낮과 동일하게 유지하며 구매 완료 상태만 카드 전체를 딤드한다.
- 2026-08-04 상점 호버 확정: 카드 전체를 약 80% 어둡게 딤드하고 그 위에 이름·효과·전투 수치를 중앙 배치한다. 호버 상세에서는 이미지와 구매 가격을 제거한다.
- 2026-08-04 상점 레이어 확정: 호버 상세 레이어를 구매 완료 상태 레이어보다 위에 표시한다.
- 2026-08-04 상점 골드 부족 표시 변경: 카드 전체 딤드를 제거하고 가격 캡슐·동전·가격 글자만 그레이스케일로 표시한다. 구매 완료 딤드는 유지한다.
- 2026-08-04 데이터 재동기화: 최신 읽기 전용 `turret` 시트의 `DOT range=0.2/rangeValue=1.5`, `STUN range=2`, `SLOW range=2`를 모든 Tier에 행 순서대로 반영했다. 나머지 로컬 테이블 값은 최신 원본과 동일함을 확인했다.
- 2026-08-05 데이터 갱신: 읽기 전용 원본의 최신 터렛 피해·가격·CC 수치와 몬스터 체력·이동 속도를 행 순서대로 다시 반영했다. `wave4` 보스 수는 기획자가 입력 실수로 확인한 4기에서 최신 원본의 1기로 변경했다.
- 2026-08-08 데이터 갱신: 최신 읽기 전용 원본을 정본으로 적용해 Tier 1 터렛 5종의 가격을 20으로 변경하고 몬스터 체력·보상 골드를 갱신했다. SpawnTable은 6행으로 재구성했으며 `wave4`는 `normal2`, `spped2`, `tank2` 각 300기로 변경했다.
- 2026-08-04 보상 피드백 확정: 몬스터 처치 시 데이터의 `rewardGold`를 사용해 사망 위치에 작은 동전 애니메이션을 표시하고 골드 UI에 `+ n G`를 짧게 표시한다.
- 2026-08-04 UI 글자 정렬 확정: 보유 골드와 획득량은 골드 캡슐 중앙축에, 동전과 구매 가격은 합산 폭 기준으로 가격 캡슐 중앙에, `WAVE`와 현재/전체 숫자는 원형 배지 중앙에 각각 정렬한다.
- 2026-08-04 골드 획득 표시 보정: 보유 골드는 캡슐 중앙에 고정하고 `+ n G`는 같은 수평 중심에서 시작해 판넬 경계에 잘리지 않고 위로 상승·소멸한다.
- 2026-08-08 설치 위치 표시 변경: 평상시 빈 설치 위치는 30% 불투명도로 표시하고 포탑이 설치된 위치는 0%로 숨긴다. 설치된 타워의 선택·이동 또는 상점 카드 구매 드래그 중에는 유효한 빈 슬롯과 머지 대상을 60%로 강조하며, 조작 종료 즉시 빈 슬롯은 30%, 점유 슬롯은 0%로 복원한다.
- 2026-08-04 UI 배치 확정: 상·하단 전체 폭 판넬을 제거하고, 상점 제어부와 카드의 개별 판넬만 유지한다.
- 2026-08-04 UI 배치 확정: 좌측 상단은 낮·밤 풍경 이미지가 교차하는 반원형 게이지와 중앙 원형 `WAVE 현재/전체` 배지로 교체한다. `최심부 코어 침입` 문구는 표시하지 않는다.
- 2026-08-04 UI 그래픽 확정: 러프 참고 이미지를 그대로 복제하지 않고 현재 배경의 색면·외곽선·명암 수준에 맞춘 낮·밤 HUD 프레임을 사용한다. 2026-08-08 교체본은 `assets/ui/images/top_1.png`·`top_2.png`의 풍경과 중앙 배지를 그대로 사용한다.
- 2026-08-08 UI 이미지 교체 확정: 테스트 옵션·데이터테이블 컨트롤과 `GAME OVER`·`GAME CLEAR` 결과 화면은 유지하고, 그 외 공통·일반 플레이 UI는 `assets/ui/images`의 `top`·`frame`·`btn`·`icon`·`coin`·`dimmed`·`ribbon` PNG 조합으로 교체한다.
- 2026-08-04 검증 절차 확정: 코드·데이터·입력을 바꾸지 않는 단순 이미지 작업은 에셋 임포트·장면 파싱·Web 표시 등 핵심 검증만 수행하고 관련 없는 회귀 테스트는 생략한다.

## 불일치 및 검증 필요

- 전체 5웨이브 설정과 4웨이브까지만 존재하는 SpawnTable의 관계
- 4웨이브 보스 4기 설정의 의도 여부
- 상점 카드 4개와 터렛 5종 데이터의 관계
- `20260804 REQ-DESIGN-001` 답변의 “다른 터렛”이 서로 다른 `turretId` 머지까지 뜻하는지와, 그렇다면 결과 `turretId`를 정하는 규칙 (`Design Request/20260805 REQUEST.md`)

## 변경 기록

- 2026-08-08: 빈 일러스트 슬롯과 `게임 시작`·`옵션`만 표시하는 타이틀 화면을 기본 시작 장면으로 확정하고 타이틀에서 테스트 환경으로 직접 들어가는 사용자 UI를 금지함.
- 2026-08-08: 게임 클리어 화면의 타이틀 아래 중앙에 `다시하기` 버튼을 추가하고, 전체 플레이 상태를 초기화해 새 낮 단계에서 재개하도록 확정함.
- 2026-08-08: B1~B3 포탑 슬롯 전체를 화면 너비의 20%인 `384px`만큼 왼쪽으로 이동하고 기존 `240px` 균등 간격을 유지함.
- 2026-08-08: 적의 지상·B1~B3 경로를 화면 오른쪽 밖 `x=1980`에서 왼쪽 밖 `x=-60`까지 사용하도록 늘리고, 세 층의 포탑 슬롯을 화면 중앙 50% 안에 균등 배치함.
- 2026-08-08: 휠·위/아래 방향키와 네 카메라 정지점을 사용하는 두 층 뷰를 폐지하고, 지상과 B1~B3가 모두 보이는 `1920×1080` 고정 전체 뷰로 변경함.
- 2026-08-07: 설치 터렛 판매·골드 환급·판매 구역 안내와 관련 임시 데이터·자동 테스트를 제거하고, 상점 영역의 잘못된 드롭은 원래 슬롯으로 복귀하도록 확정.
- 2026-08-06: 승인된 금색 `GAME CLEAR` 생성 이미지를 사용한 편집 가능 결과 오버레이를 추가하고 승리 단계에 연결함.
- 2026-08-03: 기본 문서 구조 생성. 게임 규칙은 추가로 확정하지 않음.
- 2026-08-04: 캐주얼 RPG 광산 배경 에셋과 배경 기준 전장 좌표를 그래픽 분기에 추가함.
- 2026-08-04: 전장과 고정 UI를 분리하고 두 층 수직 카메라, 휠·방향키 입력과 네 카메라 정지점을 추가함.
- 2026-08-06: 두 층의 위·아래 여백을 확실히 확보하도록 전장 카메라를 원본 비율의 90%로 균일 축소함. 배경·몬스터·터렛 등 모든 전장 이미지는 가로·세로에 같은 배율을 적용해 비율을 유지하고 HUD·상점은 기존 크기로 고정함. 축소로 생기는 좌우 여백은 배경 원본 가장자리 조각을 동일 배율로 거울 연장해 채움.
- 2026-08-06: 두 층 카메라에서 아래층 발판과 상점 사이의 불필요한 동굴 배경 여백을 위층 표시 공간으로 전환하도록 전장을 화면 아래쪽으로 130px 이동함. `하늘+지상` 첫 화면과 90% 균일 축소 비율은 유지함.
- 2026-08-04: 전장 배경과 전투 오브젝트 마스크를 분리함. 배경은 상점 카드 사이로 화면 바닥까지 이어지고, 터렛·몬스터만 상점 위에서 잘리며, B3 아래에는 원본 하단 광산 구간을 연장해 최하단 카메라의 배경 잘림을 제거함.
- 2026-08-04: 전장 배경에 별도의 `1920×1080` 기준 클리핑을 적용해 상점 카드 틈의 배경은 유지하면서 세로로 긴 브라우저의 화면비 바깥에 추가 전장·창고가 노출되지 않게 함.
- 2026-08-04: 최심부 코어 시각 오브젝트를 제거하고 B3 왼쪽 끝의 보이지 않는 도달 판정으로 패배 조건을 유지함.
- 2026-08-04: 원본 배경의 하늘 구간을 위쪽으로 확장하고 `넓은 하늘+지상` 카메라 정지점을 기존 세 단계 위에 추가함.
- 2026-08-04: 배경 v4에서 지상 오른쪽 동굴 입구를 숲 스폰 길로 교체하고 지상 왼쪽 끝에 B1 하강 통로를 추가함. 하늘 구간은 가로·세로 동일 배율로 렌더링하고 남는 높이는 단색 하늘로 연장해 달과 구름이 타원으로 왜곡되지 않게 함.
- 2026-08-04: 같은 구도의 낮·밤 배경 텍스처와 교차 페이드 전환을 그래픽 분기에 추가함.
- 2026-08-04: 낮·밤 배경의 세부 묘사와 명암 단계를 단순화하고, Jua 글꼴과 캐주얼 RPG형 HUD·상점·버튼·슬롯 스타일을 적용함.
- 2026-08-03: 지상 비전투 진입 구간, 지하 3개 전투층, 최심부 도달 시 패배 규칙 확정.
- 2026-08-03: 전투층별 고정 터렛 슬롯 5개, 터렛 체력, 하단 카드 상점 UI 확정.
- 2026-08-03: Godot 4.7.1 단일 스레드 Web Release 빌드 생성. Noto Sans KR을 포함해 브라우저 한글 표시, 구매·배치, 웨이브 시작을 로컬 HTTP 환경에서 검증.
- 2026-08-03: 모든 지하 전투층의 이동 방향을 오른쪽에서 왼쪽으로 통일하고 최심부 코어를 B3 왼쪽 끝으로 이동.
- 2026-08-03: DB 컬럼 명세 v3를 기준으로 데이터 구조를 확정. 매 웨이브 전 정비 단계와 누적 리롤 비용을 반영하고, `failAllowedMonster`는 현재 미사용으로 결정.
- 2026-08-03: Web 캔버스 크기 변화에 맞춰 기준 화면 비율을 유지하고 게임 전체를 가운데 배치하는 반응형 화면 정책을 적용.
- 2026-08-03: 기준 화면을 `1920×1080`으로 변경하고 16:9 전장·상점 UI를 재배치. 장식 텍스트와 진행 화살표를 제거.
- 2026-08-03: 상점 카드의 기본/호버 정보 구조를 확정하고, 정비 시간 중 같은 층 빈 슬롯으로만 가능한 터렛 드래그 이동 규칙을 확정.
- 2026-08-03: 골드 표시와 누적 비용 리롤 버튼을 상단 HUD에서 하단 상점 전용 제어 영역으로 이동.
- 2026-08-03: 일반 플레이 상점 RNG를 무작위화하고 자동 테스트 및 `--shop-seed=<숫자>` 디버그 시드는 재현 가능하게 유지.
- 2026-08-03: 상점 클릭 후 슬롯 클릭 구매를 상점 카드에서 빈 슬롯으로 드래그하고 유효 드롭 시 결제·설치하는 방식으로 변경.
- 2026-08-03: 웨이브 중 상점 구매·빈 슬롯 배치·리롤을 허용하고, 리롤 가능/골드 부족 상태의 시각 구분을 강화.
- 2026-08-03: 공개 데이터 테이블을 읽기 전용 기준 원본으로 지정하고, 누락·충돌은 `Design Request/20260803 REQUEST.md`에서 기획자 요청 및 임시 처리 상태로 관리.
- 2026-08-03: 전투층 슬롯 5개를 몬스터 도착 방향인 왼쪽 65%에 재배치하고 오른쪽 35%를 진입 여백으로 확보.
- 2026-08-03: 타워 간 전투 연계를 강화하기 위해 슬롯 배치 영역을 왼쪽 50% 이내로 축소하고 슬롯 간격을 150px로 조정.
- 2026-08-03: 터렛 사거리가 슬롯 간격 단위인 점을 반영해 배치 영역을 왼쪽 60% 이내, 슬롯 간격을 180px로 재조정.
- 2026-08-03: Spawn Order 그룹 간 시간값이 자료에 없음을 확인하고 임시 10초 간격과 기획자 확인 요청을 추가.
- 2026-08-04: 근접 포탑이 경로와의 수직 좌표 차이로 공격하지 못하던 문제를 해결하기 위해 같은 층 사거리 판정을 수평 거리 기준으로 변경.
- 2026-08-04: 웨이브 클리어 시 생존 터렛 전체 회복과 전투 중 `×1`/`×2`/`×3` 게임 속도 UI 규칙을 추가.
- 2026-08-04: 새 분기에서 데이터에 없는 몬스터 공격과 터렛 체력·사망·회복을 제거하고 준비/웨이브 단계를 낮/밤으로 명명, 기본 색조 전환을 추가.
- 2026-08-04: 낮·같은 층·동일 ID와 Tier 조건의 수동 드래그 머지 규칙, 상위 데이터 스탯 적용, Tier별 더미 외형과 승급 이펙트를 확정. 층간 및 밤 머지는 기획 요청으로 남김.
- 2026-08-04: 점유 슬롯 구매 규칙을 확장해 상점 카드와 동일 ID·Tier 설치 터렛의 직접 머지를 낮·밤 모두 허용. 설치 터렛 간 밤 머지는 계속 금지.
- 2026-08-05: 기획자 답변을 반영해 동일 ID·Tier 설치 터렛의 층간 머지와 밤 수동 머지를 허용. 밤의 빈 슬롯 이동과 낮의 층간 빈 슬롯 이동은 유지 금지.
- 2026-08-04: 최신 데이터 테이블의 4웨이브, 리롤 증가값 5, Spawn Order 간격 0.5초와 고유한 wave3 순서를 로컬 데이터에 동기화.
