# 기획자 요청 및 데이터 이슈

이 문서는 기준 데이터 테이블에서 발견된 누락, 충돌, 이상값과 기획자에게 확인하거나 수정 요청할 사항을 관리한다.

- 기준 데이터 테이블: `https://docs.google.com/spreadsheets/d/1DNYhAyOK-8hTO4hKY_9cGGzvM-DoM99ttiObfkve59s/edit?usp=sharing`
- 데이터 테이블은 읽기 전용이며 Codex가 직접 수정하지 않는다.
- 새 이슈에는 발견 위치, 문제, 기획자 요청, 현재 임시 처리, 영향과 상태를 기록한다.
- 상태는 `기획 확인 요청`, `임시 처리 중`, `반영 완료`, `요청 철회` 중 하나를 사용한다.

## REQ-DATA-001 · `rerollPlusCost` 누락

- 발견 위치: `define`
- 이슈: 현재 게임의 누적 리롤 비용 계산에 필요한 `rerollPlusCost`가 데이터 테이블에 존재하지 않는다.
- 기획자 요청: 누적 리롤 비용을 유지할지 확인하고, 유지한다면 `define`에 `rerollPlusCost` 값을 추가해 달라.
- 현재 임시 처리: 기존 프로토타입 값 `rerollPlusCost=5`를 로컬 데이터에 유지한다.
- 영향: 리롤 비용이 `10 → 15 → 20…`으로 증가한다.
- 상태: `기획 확인 요청` — 2026-08-03 사용자 승인으로 임시 `+5` 유지

## REQ-DATA-002 · 전체 웨이브 수와 스폰 데이터 불일치

- 발견 위치: `define.totalWaveCount`, `spawntable`
- 이슈: `totalWaveCount=5`지만 `spawntable`에는 `wave1`부터 `wave4`까지만 존재한다.
- 기획자 요청: `wave5` 스폰 구성을 제공하거나 전체 웨이브 수를 4로 변경할지 확정해 달라.
- 현재 임시 처리: 로컬 프로토타입의 `totalWaveCount`를 4로 설정한다.
- 영향: 프로토타입은 4웨이브 완료 시 승리한다.
- 상태: `기획 확인 요청` — 2026-08-03 사용자 승인으로 임시 4웨이브 구성

## REQ-DATA-003 · `wave3`의 중복 `spawnOrder`

- 발견 위치: `spawntable.wave3`
- 이슈: `tank1`의 `spawnOrder=1`과 `spped1`의 `spawnOrder=4`가 각각 여러 행에서 반복된다.
- 기획자 요청: 중복 순서가 의도된 것인지 확인하고, 최종 스폰 순서를 고유한 값으로 제공해 달라.
- 현재 임시 처리: `wave3`은 `spawnOrder`로 재정렬하거나 행을 병합하지 않고 데이터 테이블의 행 순서를 그대로 실행한다.
- 영향: 표에 작성된 탱크/속도 몬스터 묶음이 행 순서대로 반복된다.
- 상태: `기획 확인 요청` — 2026-08-03 사용자 승인으로 행 순서 유지

## REQ-DATA-004 · 확정 전투 기능용 필드 누락

- 발견 위치: `turret`, `monster`
- 이슈: 터렛 체력과 몬스터의 터렛 공격 기능에 필요한 `maxHp`, `attackDamage`, `attackIntervalSec`, `attackRange`가 데이터 테이블에 없다. 이름 컬럼도 현재 비어 있다.
- 기획자 요청: 위 전투 필드와 표시 이름을 정식 데이터 컬럼으로 추가하거나 별도 테이블로 제공해 달라.
- 현재 임시 처리: 로컬 JSON의 `prototypeExtensions`에 명시적인 `PLACEHOLDER` 값과 표시 이름을 유지한다.
- 영향: 기능 검증은 가능하지만 해당 수치는 데이터 테이블의 최종 밸런스로 간주할 수 없다.
- 상태: `기획 확인 요청`

## REQ-DATA-005 · 보스 처치 보상값 확인

- 발견 위치: `monster.boss1.rewardGold`
- 이슈: `boss1`의 처치 보상이 `555555` 골드로 다른 몬스터와 큰 차이가 있다.
- 기획자 요청: 의도된 값인지 확인해 달라.
- 현재 임시 처리: 데이터 테이블 원문 값 `555555`를 로컬 데이터에 그대로 반영한다.
- 영향: 보스 처치 시 프로토타입 경제가 사실상 무제한 상태가 된다.
- 상태: `기획 확인 요청`

## REQ-DATA-006 · Spawn Order 그룹 간 시간 간격 누락

- 발견 위치: PDF `Define`, `Spawntable` 명세 및 데이터 테이블 `define`, `spawnTable`
- 이슈: `monsterSpawnInterval=0.4`는 같은 Spawn Order 안에서 생성되는 개체 간 간격으로 정의되어 있지만, 서로 다른 Spawn Order 그룹 사이의 대기시간은 별도 필드나 설명으로 제공되지 않았다.
- 기획자 요청: Spawn Order 그룹 사이의 확정 간격과 이 값이 기존 개체 간격을 대체하는지 추가되는지 명시하고, 필요하면 `define`에 `spawnOrderIntervalSec` 필드를 추가해 달라. 또한 그룹 대기시간만으로 `waveTimeSec=50`을 넘을 수 있으므로 `waveTimeSec`이 강제 종료 제한인지 단순 목표 시간인지 확정해 달라.
- 현재 임시 처리: 로컬 `prototypeExtensions.spawnOrderIntervalSec=10`을 사용한다. 같은 Spawn Order 내부 개체는 0.4초 간격으로 생성하고, 다음 행의 `spawnOrder` 값이 달라질 때는 마지막 개체 이후 10초 뒤 다음 그룹을 시작한다.
- 영향: 웨이브의 전체 진행 시간이 길어지며, 원본 행 순서를 유지하는 `wave3`은 `spawnOrder=1`과 `4`가 바뀔 때마다 10초 간격이 반복 적용된다. 현재 프로토타입은 모든 스폰과 전투가 끝날 때까지 웨이브를 진행하며 `waveTimeSec`으로 강제 종료하지 않는다.
- 상태: `기획 확인 요청` — 2026-08-03 사용자 지시로 임시 10초 적용
