# NAN 2026 머지 디펜스 게임 개발 지침

## 1. 프로젝트와 문서 역할

이 저장소는 NHN `NAN 2026 Game X AI Hackathon` 사전 과제용 Godot 게임을 개발한다.

- 엔진: Godot 4 계열
- 언어: GDScript
- 목표 플랫폼: 브라우저에서 바로 실행되는 Web 빌드
- `AGENTS.md`는 작업 경로, 구현 원칙, 검증, Git 및 제출 절차 같은 **개발 지침**을 관리한다.
- `DESIGN.md`는 게임 목표, 핵심 루프, 전장, 전투, 상점, UI, 밸런스 초안과 미확정 항목 같은 **게임 기획**을 관리한다.
- `Design Request/YYYYMMDD REQUEST.md`는 데이터 테이블 누락·충돌·이상값처럼 기획자에게 확인하거나 수정을 요청할 항목과 현재 임시 처리 상태를 일자별로 관리한다.
- 게임 코드, UI, 데이터 또는 플레이 규칙을 제작·수정하기 전에 반드시 `DESIGN.md`를 읽는다.
- 확정 날짜와 상세 변경 이력은 `docs/GAME_SPEC.md`를 함께 참고한다.

실제 개발 저장소와 Git 기준 루트는 `<repo>`이며, Godot 프로젝트는 `<repo>/game/` 아래에 둔다.
현재 개발에서 사용하는 유일한 Godot 프로젝트 파일은 `<repo>/game/project.godot`이다. 별도의 `project.godot`을 저장소 루트나 다른 폴더에 만들지 않는다.

## 2. 개발 우선순위

다음 순서로 작업한다.

1. 실행 가능한 기본 게임 루프
2. 적 이동, 터렛 배치, 공격, 피격, 사망
3. 상점, 구매, 머지, 골드
4. 웨이브 진행, 승리와 패배
5. 플레이 피드백과 UI 정리
6. 데이터 검증과 밸런싱
7. Web 빌드 및 제출 안정성
8. 추가 연출과 선택 기능

AI 공세 디렉터는 후순위 선택 기능이다. 사용자가 별도로 구현을 요청하기 전에는 개발하지 않는다. 기본 게임 완성과 Web 제출 일정에 부담을 주면 생략한다. 규칙 기반 또는 유틸리티 기반 시스템을 머신러닝이라고 표현하지 않는다.

## 3. 문서 확인과 추측 금지

- 기획 자료의 수치, 화면, 웨이브는 초기 구상 또는 플레이스홀더로 취급한다.
- 확인되지 않은 규칙이나 수치를 임의로 최종 확정하지 않는다.
- 자료끼리 충돌하거나 사용자 명령이 `AGENTS.md` 또는 `DESIGN.md`와 충돌하면 한쪽을 자의적으로 선택하지 않는다.
- 충돌을 발견하면 관련 구현을 중단하고, 충돌 항목을 번호별로 `문서 현행 원문 / 새 명령 / 예상 영향` 형식으로 정리해 사용자에게 알린다.
- 각 충돌 항목에 대해 사용자가 `원문 유지` 또는 `새 명령 반영`을 판단하기 전에는 해당 내용을 구현하거나 관련 문서를 수정하지 않는다.
- 사용자가 새 명령 반영을 승인하면 개발 지침 변경은 `AGENTS.md`에, 게임 기획 변경은 `DESIGN.md`와 `docs/GAME_SPEC.md`에 먼저 반영한 뒤 구현한다.
- 기준 데이터 테이블에서 누락, 충돌, 이상값 또는 추가 컬럼 필요를 발견하면 `Design Request/`의 현재 활성 요청 파일에 이슈 내용, 기획자 요청 사항, 현재 임시 처리와 상태를 기록한다.
- 사용자가 확정하지 않은 내용은 `TODO`, `PLACEHOLDER` 또는 개발용 플래그로 표시한다.
- 프로토타입의 전투 수치는 기능 검증을 위해 임의로 지정할 수 있다. 다만 최종 밸런스로 간주하지 않고 `PLACEHOLDER`로 명시하며 교체 가능한 데이터로 분리한다.
- 사용자 파일이나 팀원이 만든 리소스를 확인 없이 덮어쓰거나 삭제하지 않는다.
- 기능 구현에 꼭 필요하지 않은 대규모 리팩터링은 피한다.

현재 확정된 게임 규칙과 미확정 항목은 `DESIGN.md`에서 관리한다.

### REQUEST 일일 파일 규칙

- 요청 문서는 `<repo>/Design Request/` 아래에서 관리하고 파일명은 `YYYYMMDD REQUEST.md` 형식을 사용한다.
- 날짜 경계와 시간대는 한국 시간(`Asia/Seoul`, KST) 매일 04:00이다.
- 04:00 이상인 시각에는 당일 날짜 파일을, 04:00 미만인 시각에는 전날 날짜 파일을 현재 활성 요청 파일로 사용한다.
- 각 파일은 파일명 날짜의 04:00부터 다음 날 03:59:59까지 발생한 기획자 요청 사항을 기록한다.
- 해당 구간에서 처음 요청 문서를 사용할 때 활성 파일이 없으면 새로 만든다.
- 새 04:00 구간이 시작되면 이전 날짜의 요청 파일은 읽기 전용 이력으로 취급하며 수정, 상태 갱신, 내용 보강을 하지 않는다.
- 이전 이슈의 후속 내용이나 상태 변경이 새 구간에 발생하면 이전 파일을 고치지 않고 현재 활성 파일에 원본 파일명과 이슈 ID를 참조해 새 기록을 남긴다.
- 이전 요청 파일은 맥락 확인을 위해 읽을 수 있지만, 과거 내용을 현재 파일로 자동 복사하지 않는다.

## 4. Godot 프로젝트 규칙

- Godot 4와 GDScript를 사용한다.
- 게임 코드와 리소스 경로는 `res://`를 사용하며 개인 PC의 절대 경로를 넣지 않는다.
- Web 빌드를 고려해 Compatibility 렌더러를 기본으로 유지한다.
- Web에서 검증되지 않은 GDExtension은 사용하지 않는다.
- 게임플레이 구현 방식과 이동 상태는 `DESIGN.md`의 기술 기획을 따른다.
- 첫 화면에는 사용자의 클릭을 받는 시작 버튼을 두어 브라우저 오디오 제한에 대응한다.
- 고정 RNG 시드 또는 디버그 시드를 제공해 버그와 영상 장면을 재현할 수 있게 한다.

권장 구조:

```text
<repo>/
  AGENTS.md
  DESIGN.md
  Design Request/
    YYYYMMDD REQUEST.md
  game/
    project.godot
    scenes/
    scripts/
    data/
    assets/
    tests/
  docs/
    GAME_SPEC.md
    PROJECT_STATUS.md
```

## 5. 데이터와 밸런스

- 기준 데이터 테이블은 `https://docs.google.com/spreadsheets/d/1DNYhAyOK-8hTO4hKY_9cGGzvM-DoM99ttiObfkve59s/edit?usp=sharing`이다.
- 기준 데이터 테이블은 **읽기 전용 원본**으로 취급하며 셀, 시트, 서식, 권한, 댓글을 직접 또는 API·자동화 도구로 수정하지 않는다.
- 프로토타입에는 기준 데이터 테이블을 읽어 만든 로컬 JSON/CSV/Resource 사본만 사용한다.
- 전투 수치를 스크립트에 직접 하드코딩하지 않는다.
- 테스트 수치는 교체 가능한 JSON, CSV 또는 Godot Resource로 분리한다.
- `Define`, `Turret`, `Monster`, `SpawnTable`, `ShopGacha`를 별도 데이터 구조로 유지한다.
- 각 데이터 구조의 게임 필드와 현재 플레이스홀더 값은 `DESIGN.md`를 따른다.
- 런타임에 Google Sheets를 호출하지 않는다. 제출 빌드는 네트워크 없이 동작해야 한다.
- 데이터 로더는 잘못된 ID, 누락된 다음 Tier, 확률 합계, 중복 순서, 존재하지 않는 웨이브를 검사한다.
- 공격 속도 원본 값은 `N초당 1회 공격`이므로 코드에서는 `attack_interval_sec`처럼 의미가 분명한 이름을 사용한다.
- 원본 ID의 오탈자는 사용자 확인 없이 바꾸지 않고 표시 이름과 내부 ID를 분리한다.
- 구조 검증과 밸런싱을 분리한다. 먼저 로딩·전투·머지가 동작하게 한 뒤 플레이테스트로 수치를 조정한다.

## 6. Git과 파일 관리

- Git 저장소 루트는 `<repo>/`로 유지하고, Godot 프로젝트 루트는 `<repo>/game/`로 한다.
- `.godot/`, 임시 캐시, 로그, 개인 IDE 설정, 내보내기 자격 증명은 커밋하지 않는다.
- 커밋은 작고 명확한 기능 단위로 나눈다.
- 사용자 변경과 무관한 파일을 한 커밋에 섞지 않는다.
- `git reset --hard`, 강제 푸시, 대규모 삭제 같은 파괴적 작업은 사용자의 명시적 요청 없이 수행하지 않는다.
- 외부 에셋을 추가할 때 출처, 제작자, URL, 라이선스, 사용 범위를 즉시 기록한다.
- 비밀 키, 토큰, 개인 자격 증명을 저장소에 넣지 않는다.

### GitHub 작업 표준

- 이 저장소의 GitHub 관련 조회·이슈·PR·병합·Actions·Pages 작업은 GitHub CLI를 기본 경로로 사용한다. Windows에서는 `C:\Program Files\GitHub CLI\gh.exe`를 직접 호출해 `PATH` 차이로 인한 재시도를 피한다.
- 원격 브랜치 fetch·push 같은 Git 전송은 기존 `git` 명령을 사용하고, GitHub 서비스 기능은 위 `gh.exe`로 처리한다.
- GitHub 작업을 시작할 때 `gh auth status -h github.com`을 한 번만 확인한다. 인증이 정상이면 같은 작업 흐름에서 반복 확인하지 않는다.
- 인증이 실패하면 GitHub 앱이나 인앱 브라우저 쓰기 경로를 순환 시도하지 않는다. 사용자에게 `gh auth login -h github.com` 실행을 요청하고, 완료 후 `gh auth status -h github.com`을 한 번 재확인한다. 브라우저 로그인은 CLI 인증 완료로 간주하지 않는다.
- 현재 연결된 GitHub 앱은 저장소 조회 보조용으로만 취급한다. 이슈·PR 생성처럼 쓰기 권한이 필요한 작업은 처음부터 인증된 `gh.exe`를 사용한다.
- 커밋·푸시·PR·병합·배포에 대한 기존 사용자 승인 규칙은 그대로 유지한다. 사용자가 전체 과정을 한 번에 명시적으로 승인하면 중간 단계마다 같은 승인을 다시 요청하지 않는다.
- 병합과 배포를 완료하면 PR 링크, 병합 방식, 병합 커밋 SHA, Actions/Pages 상태와 최종 플레이 URL을 함께 보고한다.
- 여러 작업 채팅의 현재 상태와 인수인계는 `docs/PROJECT_STATUS.md`에서 짧게 관리한다. 작업을 시작할 때 관련 행만 확인하고, 브랜치·PR·배포·제출물 상태가 바뀌면 같은 작업에서 해당 문서를 갱신한다.

### 데이터 테이블 전용 브랜치 규칙

- 기준 데이터 테이블의 최신 내용을 로컬 프로젝트에 반영하는 작업은 사용자의 별도 브랜치 명령을 기다리지 않고 항상 전용 브랜치를 먼저 생성하거나 해당 작업의 기존 전용 브랜치로 전환한다.
- 새 데이터 테이블 브랜치는 최신 `origin/main`에서 만들고 `agent/data-table-<주제>-YYYYMMDD` 형식으로 이름을 정한다. 다른 기능 브랜치에서 분기하지 않는다.
- 데이터 테이블 전용 브랜치에는 읽기 전용 원본 조회, 로컬 데이터 사본 갱신, 데이터 필드 매핑·로더 호환 작업, 데이터 검증 테스트, 데이터 문제로 직접 발생한 당일 `REQUEST.md` 기록만 포함한다.
- 그래픽, UI, 연출, 일반 전투 기능, 무관한 리팩터링, Web 빌드 결과물 등 데이터 테이블과 직접 관계없는 변경은 데이터 테이블 전용 브랜치에 커밋하지 않는다.
- 데이터 테이블 작업 도중 그래픽·UI·기능 등 다른 명령이 들어오면 데이터 변경을 전용 브랜치에 안전하게 보존한 뒤, 최신 `main`에서 별도의 목적별 브랜치를 만들어 그 명령을 수행한다. 데이터 브랜치에서 이어서 작업하지 않는다.
- 데이터 테이블 작업을 다시 시작할 때는 전용 브랜치를 최신 `origin/main` 기준으로 먼저 갱신하고, 데이터 범위 밖의 변경이 섞이지 않았는지 확인한다.
- `main` 반영 전 최신 `origin/main`을 기준으로 충돌 여부를 확인하고 관련 검증을 통과해야 한다. 충돌이 있으면 강제 병합하거나 무관한 파일을 덮어쓰지 말고 병합을 중단해 사용자에게 알린다.
- 데이터 테이블 전용 브랜치는 사용자의 명시적인 병합 또는 배포 지시 전에는 `main`에 자동 병합하지 않는다.

### 이미지 생성 도구 사용 기준

- 생성형 이미지 도구는 사용자가 새 이미지, 일러스트, 아이콘, 배경, 스프라이트, 리터칭 또는 시안 제작을 명시적으로 요청했거나 요청의 의미상 이미지 제작이 분명한 작업에서만 사용한다. 사용자가 정확히 `이미지 생성`이라는 표현을 쓰지 않았더라도 새 비트맵 에셋 제작이 요청의 핵심이면 허용한다.
- 프로젝트 관리, 버그 수정, 데이터 테이블, 일반 문서 작성, 코드 수정과 일반 UI 구현 요청은 이미지 생성 권한을 포함하지 않는다. 설명용 장식 이미지나 임의 시안을 만들기 위해 생성 도구를 선제적으로 사용하지 않는다.
- 일반 UI 작업에 새 비트맵 에셋이 필요하지만 이미지 제작 요청이 명확하지 않으면 기존 승인 에셋이나 Godot 씬·리소스로 구현 가능한 표현을 우선한다. 새 이미지가 반드시 필요하면 생성하지 않고 필요한 에셋, 사용 위치와 예상 영향을 보고해 승인을 받는다.
- 사용자가 에셋 종류, 콘셉트와 시안 범위를 한 번 승인하면 그 범위 안의 변형 생성은 다시 승인받지 않는다. 승인 범위를 벗어난 다른 에셋이나 새 콘셉트는 별도 요청이 필요하다.
- 이미지 생성 결과는 아래 단순 그래픽 전용 브랜치 규칙을 따른다. 완성 이미지를 사용자에게 먼저 공유하고 실제 프로젝트 적용 승인을 받은 뒤 반영한다.

### 단순 그래픽 전용 브랜치 규칙

- 이미지 생성, 기존 이미지 교체, 단순 리터칭, 색상·명암·크기 조정처럼 게임 규칙이나 UI 배치 구조를 바꾸지 않는 그래픽 작업은 사용자의 별도 브랜치 명령을 기다리지 않고 항상 그래픽 전용 브랜치에서 진행한다.
- 새 그래픽 전용 브랜치는 최신 `origin/main`에서 만들고 `agent/graphics-<주제>-YYYYMMDD` 형식으로 이름을 정한다. 데이터 테이블 또는 다른 기능 브랜치에서 분기하지 않는다.
- 그래픽 전용 브랜치에는 이미지·벡터 에셋, Godot가 요구하는 임포트 메타데이터, 불가피한 리소스 경로 참조 수정, 출처·라이선스 기록만 포함한다.
- 게임 규칙, 데이터 테이블, UI 위치·구성, 일반 기능, 무관한 리팩터링과 Web 빌드 결과물은 그래픽 전용 브랜치에 커밋하지 않는다. 기능이나 UI 구조 변경이 함께 필요하면 최신 `main`에서 별도의 목적별 브랜치를 만든다.
- 이미지 생성 결과는 기존 승인 절차에 따라 완성 이미지를 사용자에게 먼저 공유하고 실제 프로젝트 적용 승인을 받은 뒤 반영한다.
- 확인용 임시 Web 빌드는 만들 수 있지만 `build/web/` 결과물은 그래픽 전용 커밋에 포함하지 않는다. 검증 후 Codex가 생성한 빌드 변경만 정리하고 사용자 또는 팀원의 변경은 건드리지 않는다.
- 그래픽 작업 도중 데이터·기능·UI 구성 등 다른 명령이 들어오면 그래픽 변경을 전용 브랜치에 안전하게 보존한 뒤 최신 `main`에서 별도 브랜치를 만들어 작업한다.
- `main` 반영 전 최신 `origin/main` 기준으로 충돌 여부와 에셋 참조·임포트 상태를 확인한다. 그래픽 전용 브랜치는 사용자의 명시적인 병합 또는 배포 지시 전에는 `main`에 자동 병합하지 않는다.

## 7. Web 빌드와 제출 기준

- Web 내보내기 파일명은 `index.html`로 한다.
- 첫 수직 슬라이스가 동작하는 시점부터 Web 빌드를 반복 검증한다.
- 최소 Chromium 계열 브라우저 두 종류와 로그인하지 않은 새 세션에서 플레이 링크를 확인한다.
- 심사자는 링크 클릭만으로 별도 유료 라이선스 없이 플레이할 수 있어야 한다.
- PC 실행 파일 직접 제출을 전제로 개발하지 않는다.
- 실제 플레이 영상은 30~60초이며 AI로 조작·합성하지 않는다.
- 사용한 AI 도구와 활용 내역, 외부 에셋과 오픈소스 라이선스를 제출 문서에 기록한다.
- GitHub 및 영상 링크는 심사 종료까지 접근 가능하게 유지한다.

### UI 변경 Web 배포 완료 체크리스트

- UI 변경을 배포할 때는 관련 장면·스크립트 테스트 통과만으로 완료 처리하지 않는다. 사용자가 Web 검증을 명시적으로 생략하지 않았다면 아래 순서를 한 작업 흐름에서 확인한다.
  1. 변경된 UI의 Godot 장면 파싱과 관련 전용 테스트를 통과한다.
  2. 최신 소스로 Web Release를 다시 내보내 `build/web/index.html`과 `build/web/index.pck` 등 실제 배포 산출물이 갱신됐는지 확인한다.
  3. 임시 로컬 HTTP 서버에서 초기 상태, 변경 상태와 복원 상태를 실제 Web 화면으로 확인하고 콘솔 오류·경고를 점검한다.
  4. 소스와 필요한 Web 산출물을 같은 배포 범위에 포함한다. 병합 전에 발견된 문제는 같은 브랜치와 PR에서 수정해 소스 PR, 빌드 PR, 캐시 PR로 불필요하게 분리하지 않는다.
  5. Pages 워크플로가 병합 커밋 SHA 기반의 고유 PCK 파일명을 생성하고 배포 HTML이 그 파일을 요청하는지 확인한다.
  6. 배포 완료 후 원격 PCK와 검증한 로컬 Web Release의 SHA-256 해시가 일치하는지 확인한다.
  7. 사용자가 배포 후 실제 Pages 플레이 검증을 명시적으로 생략하지 않았다면 캐시 무효화 URL에서 변경 화면과 Web 콘솔을 최종 확인한다.
- 위 체크리스트의 배포 단계는 사용자가 커밋·푸시·PR·병합·배포를 승인한 경우에만 수행한다.

## 8. 작업 방식

- 짧은 기능 단위로 구현하고 항상 플레이 가능한 상태를 유지한다.
- UI의 위치를 옮기거나 구성 요소를 추가·삭제·통합·분리하기 전에는 매우 간단한 예상 배치도를 사용자에게 먼저 제시한다.
- 사용자가 예상 배치도를 승인하기 전에는 해당 UI 배치·구성 변경을 구현하거나 Web 빌드에 반영하지 않는다. 색상·폰트·테두리처럼 위치와 구성에 영향을 주지 않는 순수 스타일 변경은 이 승인 대상에서 제외한다.
- 새 기능보다 크래시, 진행 불가, 데이터 오류, Web 실행 오류를 먼저 해결한다.
- 변경 전 관련 장면과 스크립트를 읽고 기존 사용자 작업을 확인한다.
- 변경 후 관련 장면을 실행하고 가능하면 Web 빌드에서도 확인한다.
- 코드·데이터·입력 동작을 바꾸지 않는 단순 이미지 생성·교체 작업은 에셋 임포트, 장면 파싱, Web 표시처럼 중요도가 매우 높은 검증만 수행하고 관련 없는 전체 회귀 테스트는 생략한다.
- 기능 변경을 완료할 때마다 최신 Web Release를 임시 로컬 서버에 반영해 사용자가 바로 확인할 수 있게 한다. 사용자가 명시적으로 생략을 요청한 경우만 제외한다.
- 데이터 변경 시 ID 참조, 머지 트리, 확률 합계, 웨이브 수를 검사한다.
- 이해하지 못한 생성 코드를 그대로 제출하지 않는다.
- 화면, 조작 또는 게임 규칙이 바뀌면 `DESIGN.md`와 관련 기획 문서도 함께 갱신한다.

기능 완료 조건:

- 정상 경로와 대표 실패 경로를 확인했다.
- 오류 없이 장면을 실행할 수 있다.
- 가능하면 Web 빌드에서 재현했다.
- 임시 절대 경로와 개인 자격 증명이 없다.
- 플레이어에게 필요한 상태와 결과 피드백이 표시된다.
- 관련 데이터와 문서가 함께 갱신됐다.

## 9. 공모전 기준

- 참가 및 제출 관련 최신 정보는 공식 사이트 `https://nan2026.nhn.com/`와 참가 약관 `https://nan2026.nhn.com/terms`를 우선한다.
- 일정과 운영 규정은 변경될 수 있으므로 제출 직전에 다시 확인한다.
- 주요 AI 도구의 명칭과 활용 방식을 기록한다.
- 생성형 AI, 오픈소스, 외부 API, 이미지, 폰트, 음향의 이용 조건을 확인한다.
- 타인의 저작물과 아이디어를 무단 사용하지 않는다.
